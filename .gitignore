
/jdk17/
/maven39/bin/
/maven39/boot/*.jar
/maven39/lib/*.jar
/target/*.jar
/dist/*.jar
*.jar.bak
.gradle/
build/

# sqltune-rag 프로토타입/이관본 - JDK+Maven 번들 포함(수백MB), 별도 관리 대상이라 repo에 안 둠
/sqltune-rag/
/sqltune-rag-java/
*.zip

# 루트에 잘못/임시로 생기는 사본 - 정본은 data/oracle_errors.db
/oracle_errors.db

# export_error_dictionary_to_jsonl.py 산출물 - data/oracle_errors.db에서 재생성 가능한 데이터
# 덤프라 repo에 안 둠(sqlrestapi 색인용으로 그때그때 export)
/error_dictionary_full.jsonl
/error_dictionary_sample.jsonl

# 런타임 산출물 - 앱이 돌면서 계속 바뀌어 git pull 을 막았음(2026-09-11 추적 해제).
# users.db 는 로그인 계정/세션 데이터라 환경마다 달라야 하는 파일이기도 하다.
# 주의: data/oracle_errors.db(에러사전 9,372건)는 정상 데이터이므로 절대 여기 걸리면 안 된다
#       - 그래서 *.db 같은 넓은 패턴 대신 경로를 하나씩 좁게 지정한다.
/users.db
/dist/users.db
/dbagent-java.out.log
/dbagent-java.err.log
/dist/dbagent-java.out.log
/dist/dbagent-java.err.log
/dist/dbagent-java.pid
/run.log
