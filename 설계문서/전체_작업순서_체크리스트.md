# 전체 작업 순서 체크리스트 (2026-09-25)

> 합친 문서
> - [`수정필요 및 검토 내역_체크리스트.md`](./수정필요%20및%20검토%20내역_체크리스트.md) — 요청 원본 (1~9번째 그룹)
> - [`수정필요 및 검토 내역_체크리스트_검토결과.md`](./수정필요%20및%20검토%20내역_체크리스트_검토결과.md) — 항목별 검토 (2026-09-24)
> - [`대시보드_UI개선_구현단계_체크리스트.md`](./대시보드_UI개선_구현단계_체크리스트.md) — 9번째-2 대시보드 개편 상세 단계
>
> 항목 옆 `[1-5]` 같은 번호는 요청 원본의 그룹-번호다. 대시보드 개편은 여기서는 단계 단위로만 적고, 세부 항목은 대시보드 체크리스트에서 체크한다.

## 순서를 정한 기준

1. **버그와 이미 결정된 작은 수정을 먼저** — 운영(폐쇄망)에서 바로 효과가 나고, 뒤 작업의 기준값(코어 수, 60초 기준)을 먼저 고정한다.
2. **화면 배율(4-1 90%→80%)을 화면 작업보다 먼저** — 대시보드 개편의 "1920×1040 스크롤 없음" 검증을 최종 배율로 한 번만 하기 위해서다.
3. **공통 기반을 한 번만 만든다** — 60초 샘플러 개선은 1-10(Current Session)과 대시보드 개편이 같이 쓰고, "ASH 구간 조회 + AWR 보충"은 대시보드 개편과 2번째(성능 분석)가 같이 쓴다. 따로 만들면 두 번 고치게 된다.
4. **공통 부품은 먼저 만드는 쪽에서 만들고 뒤에서 재사용** — 그래프 드래그(1-3 → 대시보드 ②), 스켈레톤·DB 전환 처리(1-2·1-5 → 대시보드), 세션 상세 팝업 스펙(4-3 → 4-2 팝업 통일 → 성능 분석 팝업).
5. **문서 작업은 코드가 안정된 뒤** — 쿼리 성능 체크 문서(5-2)와 산출물(8-1)은 마지막.

**모든 단계 공통**: 메인·AIX 동시 적용(AIX는 Java 8·H2), 단계마다 로컬 검증 → 커밋 → 작업정리 기록. `배포` 표시가 있는 곳에서 폐쇄망에 한 번씩 내보낸다.

---

## A. 선행 수정·버그 — 운영에 바로 효과

- [x] **A1. getFailureProb inst_id 버그** (대시보드 체크리스트 1-1)
  - [x] 메인·AIX 소스 수정, 로컬 Oracle 재현·검증, compile 성공 (2026-09-25)
  - [x] 커밋 (메인 `af17423`, AIX `aaeca85`)
- [x] **A2. TM Holder 60초 기준 프로퍼티화** `dbagent.monitor.tm-holder-last-call-et-seconds` (대시보드 체크리스트 1-2) — 실제 블로킹 세션으로 30/60/잘못된 값 검증 완료 (2026-09-25). AIX 런타임 테스트는 배포 전
- [x] **A3. CPU 코어 수 `NUM_CPU_CORES` 통일** `[1-1]` (대시보드 체크리스트 1-3) — (가)안 결정: 코어 기준선 2곳(Current Session 차트·샘플러 `ash_cpu_cores`)만 `NUM_CPU_CORES`, CPU% 분모는 `NUM_CPUS` 유지. 로컬 검증 32→16 (2026-09-25)
- [x] **A4. 테이블스페이스 화면 DB 전환 시 합계 값 잔존 버그** `[7-1]` (메인 `ba96d34`, AIX `5ccc82c`, 2026-09-25)
  - [x] DB 클릭 즉시 전체 할당량/사용량/사용률을 `--`로 초기화, 늦게 온 이전 DB 응답은 버림(요청 세대 번호)
  - [x] 원인 추가 발견: 이전 DB 조회 중이면 메뉴 전환 코드가 새 DB 요청을 건너뛰었음 → 조회 중인 DB와 현재 DB가 다르면 새로 요청
  - [x] 검증: 헤드리스 Chrome + CDP로 A 요청을 붙잡고 B로 전환 후 A를 가짜 값으로 늦게 응답 — 수정 전 배포본은 999,999 MB로 덮어써짐(재현), 수정본은 전환 즉시 `--`·B 값 유지
  - [x] app.js 캐시버스터 메인 171 / AIX 159
  - [x] AIX 런타임 테스트 (2026-09-25 배포 ① 전 통과)
- [x] **A5. Current Session DB 전환 후 그래프 갱신 지연·검은 화면** `[1-5]` (메인 `de0cc36`, AIX `1435b10`, 2026-09-25)
  - [x] 원인 확인 (수정 전 배포본으로 CDP 재현): ① DB 전환 후 새 DB 응답이 올 때까지 이전 DB 그래프·KPI가 새 DB 것처럼 표시(제목은 DB#2, 그래프는 DB#1) ② 세션 목록에 늦게 온 이전 DB 응답이 그려지는 레이스. 폴링 타이머 누락은 아님(세대 번호로 이미 관리 중)
  - [x] 공용 유틸 `dbagentLatestRequest()`(세대 번호 + 요청 시점 DB) 작성 → 세션 목록·두 그래프에 적용, 대시보드 개편에서 재사용
  - [x] DB 전환 즉시 그래프·KPI·범례 비우고 "불러오는 중…", 첫 데이터 전 실패 시 차트 위 오류 표시
  - [x] 검증: 수정본은 전환 즉시 빈 그래프+"불러오는 중…"+KPI "–", 늦은 이전 DB 응답 무시, 새 DB 응답 후 정상
  - [x] "검은 화면" — A6에서 원인 확인: ASH 응답 전 차트가 아직 없는 빈 영역(첫 응답이 느리면 그대로 검게 보임). A6 스켈레톤으로 해결. `AbortController`(요청 자체 취소)는 세대 번호로 충분해 보류
  - [x] AIX 런타임 테스트 (2026-09-25 배포 ① 전 통과)
- [x] **A6. Current Session 첫 로딩 검은 화면** `[1-2]` (메인 `c5f7381`, AIX `ba5aaac`, 2026-09-25)
  - [x] 조사 도구: `/api/ash_activity`·`/api/ash_top_sql` 응답에 서버 처리 시간 `query_ms`, 브라우저 콘솔에 2초↑ 응답을 "전체 / 서버 처리 / 네트워크·대기"로 기록, 서버 처리 3초↑는 서버 로그에도 기록
  - [ ] **폐쇄망에서 실제 원인 확인** — 배포 후 F12 콘솔의 `[DBAgent] ash_...` 로그로 서버 쿼리 vs 네트워크 판단 (오케스트레이터 현장 확인 필요)
  - [x] 그래프 틀(축·격자·제목·범례) 스켈레톤 + "불러오는 중…"을 페이지 로드 직후·DB 전환 시 즉시 표시
  - [x] 검증: 첫 ASH 응답 6초 지연 흉내 — 수정 전은 검은 빈 영역(차트 없음), 수정본은 스켈레톤, 콘솔 로그로 지연 위치 구분 확인
  - [x] AIX 런타임 테스트 (2026-09-25 배포 ① 전 통과)
- [x] **배포 ①** — A1~A6 (2026-09-25 17:39)
  - [x] AIX 런타임 테스트 (H2 설정, 로컬): 실제 블로킹 세션 판정(프로퍼티 30), 코어 16, 테이블스페이스·Current Session DB 전환, 첫 로딩 스켈레톤 모두 통과
  - [x] 두 프로젝트 `mvn clean package` → main `dist`, AIX `dist`, AIX `dist-aix` 배포 (기존 jar `.bak-20260925-1739` 백업), 배포 jar 스모크 테스트 ERROR 0건
  - [x] AIX 패치 잔여 `.orig` 파일이 jar에 들어간 것 발견 → 제거 후 재빌드·재배포 (`/app.js.orig` 404 확인)
  - [x] push (main `11206d3`, AIX `ba5aaac`) + 절차서 `가이드/폐쇄망_적용절차_2026-09-25.md` (AIX 루트·dist-aix에도 복사)
  - [ ] **폐쇄망 반영** — 오케스트레이터 현장 작업 (절차서 1~3절)
  - [ ] **폐쇄망 느린 원인 확인** — F12 콘솔 `[DBAgent] ash_...` 로그 결과 전달 (절차서 4절)

---

## B. 저위험 UI·설정 + 화면 배율

- [x] **B1. 화면 초기 크기 90% → 80%** `[4-1]` (2026-09-25, 오케스트레이터 결정 (나)안: 전체 80% 통일)
  - [x] 확인: "90%"는 팝업이 아니라 Oracle 메인 화면 배율(`.app-container` zoom). RDB 대시보드는 본문 100%·사이드바만 Oracle 0.9배에 맞춰져 있었고, FO는 100%
  - [x] Oracle `index.html` zoom 90→80%, `.app-container` 높이 `100vh/0.8`, Oracle만 0.9배라 넣었던 보정값 원복(상단 줄 54px·16px, 아이콘 18px·간격 6px)
  - [x] RDB 대시보드 4종: `.pmm-app`에 zoom 80%(+높이 1/0.8, 비밀번호 모달은 바깥), 사이드바 0.9배 값 원복(1.35rem/1rem/.95rem/16px/18px), 사이드바 폭 240→260px(Oracle과 같은 208px로 보이게)
  - [x] FO: `.page`에 zoom 80%
  - [x] 검증(메인·AIX 스크린샷): 세 화면 모두 80%, Oracle·RDB 사이드바 폭 208px 동일, RDB 모달 화면 중앙 정상. 캐시버스터 main style 48/app 174, AIX style 44/app 162
  - [ ] 해상도별 가변 배율 — 별도 검토 항목 (이번엔 80% 고정)
  - 참고: `rdb-dashboard.html`(전용 대시보드가 없는 DB 종류용 예비 화면, 현재 5종 모두 전용 화면 사용)은 변경하지 않음
- [x] **B2. Current Session Active Session 리스트 7개↑ 세로 스크롤** `[1-4]` (2026-09-25) — 4개 탭 모두, 머리글+7행 실측 높이로 제한(예전 500px≈11행), 활성 세션 9개로 검증
- [x] **B3. RDB Client Connects 프레임 9개↑ 세로 스크롤** `[3-1]` (2026-09-25) — MySQL/MariaDB·PostgreSQL·MSSQL, 머리글+8행 실측 높이, 기존 20개 제한 해제, MySQL 연결 15개로 검증
- [ ] **B4. RDB / ORACLE DB / FO 버튼 색 통일** `[3-2]`
- [ ] **B5. Current Session 리프레쉬 기본 3초** `[1-9]`
  - [x] "현재 5초" 코드 위치 확인 — `index.html:948` `session-refresh-interval` input의 `value="5"` 하드코딩 + `app.js:1807` 폴백 5, 프로퍼티 없음 (2026-09-24 확인, 착수 시 줄 번호 재확인)
  - [ ] 기본값 3초로 변경 + 프로퍼티로 빼기(기존 `dbagent.ui.polling-interval-ms`처럼 서버에서 내려주는 방식), 오케스트레이터에게 키 안내
  - [ ] 질문 답변: 리프레쉬 주기가 좌/우 그래프·리스트에 같이 적용되는지 (현재는 타이머가 분리돼 "아니오"에 가까움) — C단계 이후 동작 기준으로 최종 답변
- [ ] **B6. SQL 실행 메뉴 허용 명령어 확인** `[5-1]` — 코드 확인 후 답변만 (구현 없음)
- [ ] **B7. AI 챗봇 system prompt 변경** `[6-1]` — 정책 확정됨(ORA-코드 포함 시 Vector DB, 그 외 모델 지식·중간 리스크 허용·DB 무관 주제 허용)
- [ ] **배포 ②** — B1~B7

---

## C. 공통 기반 ① — 60초 샘플러 개선 (1-10 + 대시보드 개편 2단계 합침)

- [ ] **C1. 샘플러 경계 오차 수정** `[1-10 보완]` — "SYSDATE−60초" 창 대신 마지막으로 처리한 ASH `sample_id` 이후만 집계
- [ ] **C2. 7분류 + 8분류 동시 집계** (대시보드 체크리스트 2단계) — 기존 `ash_*` 유지 + `ash_wc_*` 8개 추가, CASE 공통 상수화
- [ ] **C3. 기동 시·신규 DB 등록 시 1시간 backfill** `[1-10 보완]` — `ash_*`·`ash_wc_*` 둘 다
- [ ] **C4. DB 시각 차이 캐시 + `/api/metric_history` 확장** — range `15m`/`30m`/`3h` 추가, `dbClockOffsetMs` 필드
- [ ] **C5. Current Session 30분/1시간도 저장 데이터로 전환** `[1-10]` — 원본 DB 조회를 인스턴스당 1분 1회로 고정
  - [ ] 트레이드오프 확인: 그래프 반영 지연 최대 약 60초. 필요 시 맨 오른쪽 진행 중 1분 칸만 리스트 결과로 덧그리기 검토
- [ ] 검증: 같은 분 7분류 합 = 8분류 합, Current Session 화면 동작 동일, `query-performance-reviewer` 검토
- [ ] **배포 ③**

---

## D. 공통 기반 ② — ASH 구간 조회 + AWR 보충 (대시보드 개편 5단계 + 성능 분석 ① 합침)

- [ ] **D1. 공용 ASH 구간 조회 서비스** — 시작/종료 시각 파라미터, 분류 선택(7분류/8분류), 모니터링 계정 제외, FOREGROUND
  - 대시보드 `ash_base`(`/top`·드로어)와 성능 분석 "getAshActivity 시작/종료 시각 파라미터화"가 같이 쓴다
- [ ] **D2. AWR 보충** — ASH 보관 범위 밖은 `dba_hist_active_sess_history`(10초 간격 환산), 응답 `source: ash|awr|mixed`
- [ ] **D3. 폐쇄망 실측** — ASH 보관 범위·AWR 보존 기간 확인 (성능 분석 검토 문서 8절 1번과 같은 항목)
- [ ] DB 시각 기준·최대 24시간·타임아웃·`ROWNUM`(11g) 확인, `query-performance-reviewer` 검토
- (배포는 E와 함께)

---

## E. Current Session 기능 추가

- [ ] **E1. 좌측 그래프 드래그 → 구간 세션 정보** `[1-3]` — 드래그 구간 선택을 공용 모듈로 작성(대시보드 ② 재사용), 조회는 D1 사용
- [ ] **E2. 우측 그래프(Top SQL Activity Timeline) 드래그 개선** `[1-3]` — 현재 세로 구간 단위가 의도인지 확인 후 결정
- [ ] **E3. 우측 그래프 색 계열 클릭 필터** `[1-6]`
- [ ] **E4. Duration Time(sec) 정렬** `[1-7]` — Current Session만
- [ ] **E5. Remote 탭 추가** `[1-8]` — Current Session만 (대시보드 탭 구성은 취소됨)
- [ ] **배포 ④** — D + E

---

## F. 대시보드 개편 `[9-2]` — 상세는 대시보드 체크리스트

A3·A2(1단계), C2·C4(2단계), D1·D2(5단계 일부)는 앞에서 끝난 상태로 시작한다.

- [ ] **F1.** 저장 테이블 5개 (대시보드 체크리스트 3단계)
- [ ] **F2.** Lock 실시간 API + KILL API (4단계) — `security-reviewer`
- [ ] **F3.** Top·드로어 API(D1 사용) + SQL 통계 델타 (5단계 나머지)
- [ ] **F4.** 프런트 뼈대 — A5·A6 공용 유틸/스켈레톤 재사용, B1 최종 배율 기준 (6단계)
- [ ] **F5.** ① KPI · ② AAS 차트(E1 드래그 모듈 재사용) · ④ 진단 (7단계)
- [ ] **F6.** ③ Lock 카드 · 장애 처리 UI (8단계)
- [ ] **F7.** ⑤⑥⑦ Top · 상세 드로어 (9단계)
- [ ] **F8.** ⑧ 점검 알림 (10단계)
- [ ] **F9.** 전체 검증 (11단계)
- [ ] **배포 ⑤** — 폐쇄망 적용 절차서 포함
- [x] ~~9-1 기존 대시보드 프레임 통합~~ — 취소 (개편으로 대체, 2026-09-25)

---

## G. 세션 상세 · 팝업 통일 · 성능 분석 `[4-3]` `[4-2]` `[2-1]`

- [ ] **G1. 세션 상세정보 리뉴얼** `[4-3]` — 표 배치(SID.SPID ~ Hash Value), 폰트 축소, 탭 문구 변경(SQL Text / DBMS Plan / Bind Variable)
- [ ] **G2. 팝업 통일 스펙 확정** `[4-2]` — 세로형 → 가로형, 환경설정 팝업 제외 전체 적용. G1 결과를 기준 스펙으로 삼는다
  - [ ] **배율 80% 기준 포함** (2026-09-25 결정): B1 이후 본문은 80%인데 팝업 창(세션 상세·세션 목록·테이블 정보·데이터파일·ERD·계정/메뉴/DB 관리)과 화면 내 모달은 100%로 남아 있음. 팝업을 새 스펙으로 만들 때 본문과 같은 80% 배율 + 그에 맞는 창 크기로 설계 (지금 따로 고치지 않음)
- [ ] **G3. 성능 분석(구 "성능 이력 조회") 개편** `[2-1]` — 설계 확정됨(`성능이력조회_개편_검토_2026-09-24.md` 9.2절 순서)
  - [ ] 메뉴명 "성능 분석"으로 변경
  - [ ] 상단 wait class 히스토리 막대(D1·D2 사용, Current Session과 같은 7분류·색)
  - [ ] 시간축 드래그(E1 모듈 재사용)
  - [ ] 하단 event별 막대 그래프
  - [ ] event 막대 클릭 → 세션 목록 팝업 (G2 스펙의 첫 적용, 필드는 `getSessions()` 기준에서 `status`/`session_wait_pct`/`has_transaction` 제외)
  - [ ] 필요 시 자체 적재
- [ ] **G4. 나머지 팝업을 G2 스펙으로 순차 교체**
- [ ] **배포 ⑥**

---

## H. 문서

- [ ] **H1. 기능별 쿼리 성능 체크 문서** `[5-2]` — 코드가 확정된 뒤 작성해야 다시 고치지 않음. `query-performance-reviewer` 결과 활용
- [ ] **H2. 산출물 디렉토리 + 프로젝트 정리 md** `[8-1]` — `deliverable-writer` 에이전트, 계속 수정 가능한 형태
- [ ] 요청 원본 체크리스트에 완료 표시 정리

---

## 진행 현황 요약

| 단계 | 내용 | 상태 |
|---|---|---|
| A | 선행 수정·버그 (1-1, 1-2, 1-5, 7-1, 60초 프로퍼티, inst_id 버그) | A1~A6 완료, 배포 ① 완료(폐쇄망 반영·원인 확인 대기) |
| B | 저위험 UI·설정 (4-1, 1-4, 3-1, 3-2, 1-9, 5-1, 6-1) | 대기 |
| C | 샘플러 개선 (1-10 + 대시보드 2단계) | 대기 |
| D | ASH 구간 조회 + AWR 보충 (대시보드 5단계 + 성능 분석 ①) | 대기 |
| E | Current Session 기능 (1-3, 1-6, 1-7, 1-8) | 대기 |
| F | 대시보드 개편 (9-2) | 설계 완료, 구현 대기 |
| G | 세션 상세·팝업 통일·성능 분석 (4-3, 4-2, 2-1) | 성능 분석 설계 완료, 대기 |
| H | 문서 (5-2, 8-1) | 대기 |
