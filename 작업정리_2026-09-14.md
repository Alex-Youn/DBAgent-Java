# 작업정리 2026-09-14

> 대상: `DBAgent-Java`/`DBAgent-Java-AIX` (전 항목 양쪽 동기화, `com.dbagent.aidba` 제외 원칙 유지)
> 상태: 전부 완료 · 빌드/배포(`dist` 2곳 + `dist-aix`) 및 코드 리뷰까지 반영.

---

## 1. Session Wait 색상 정합성

- 그래프와 세션 리스트의 Wait 색이 서로 안 맞던 문제 확인 후 정리:
  - 트렌드 그래프에서 2PC Pending 제거.
  - "Lock Wait" 하나로 뭉쳐 있던 걸 `v$lock.type`(`TX`/`TM`) 기준으로 TX Lock / TM Lock 두 계열로
    분리(양쪽 그래프 공통 반영).
- 처음 정한 TX(핑크)/TM(빨강)이 구분이 잘 안 된다는 지적 → `dataviz` 스킬의 OKLab ΔE 검증 로직을
  Node가 없어 Python으로 포팅해서 후보를 계산·검증.
  - 최종 색: **TX Lock `#7c3aed`**, **TM Lock `#be123c`**.
  - 이후 CPU 색과 TX Lock이 새로 부딪혀서 **CPU `#22d3ee`**로 재조정.

## 2. Lock Holder/Waiter Tree 체크박스 오정렬 버그

- 자동 리프레시 후 체크된 세션 표시가 엉뚱한 행으로 옮겨붙는 증상 → **원인: Oracle SID 재사용**
  (SID만으로 세션을 식별하던 로직이 시간이 지나면 다른 세션과 혼동됨).
- SID 단독 키 → **SID+SERIAL# 복합 키**로 교체(양쪽 프로젝트 각 3곳: TM Lock 트리, 세션 리스트,
  대시보드 체크박스 복원 로직).

## 3. 성능 이력 조회(History) 필터 개선

- 모니터링 계정이 이력 조회에 그대로 섞여 나오던 문제 → 계정 필터에서 모니터링 계정 제외.
- WAS 서버 단위 필터 신규 추가: ASH(`v$active_session_history`) + AWR
  (`dba_hist_active_sess_history`, 최근 7일)에서 실제 관측된 `machine` 값만 뽑아 드롭다운 구성.
- 계정 드롭다운도 같은 방식으로 "실제 ASH에서 관측된 계정만" 나오도록 변경
  (`getDbUsers`→`getHistoryUsers`, `/api/db_users`→`/api/history_users`).

## 4. GPU 서버 연동 설정 통합: `aidba.restapi.url`

- `aidba.ollama.url` + `sqltuneadvisor.api.url` 두 프로퍼티를 **`aidba.restapi.url`** 하나로 통합
  (둘 다 결국 같은 `sqlrestapi` 서버를 가리키므로). 스킴이 없으면 `@PostConstruct`에서 자동으로
  `http://`를 붙여줌.
- `aidba.ollama.timeout-ms` / `sqltuneadvisor.api.timeout-ms`는 용도가 달라 그대로 분리 유지.
- `aidba.ollama.model`은 이번에 완전히 제거 확인(더 이상 참조하는 코드 없음).
- 양쪽 프로젝트의 `application.properties` / `.sample` 전부 갱신. AIX `dist-aix`용 샘플은
  2026-09-11 sqlrestapi 전환 이전 내용(직접 Ollama:11434 접속 등)이 그대로 남아 있던 걸 발견해서
  전면 재작성.

## 5. AI DBA 메뉴 UI/기능 — AIX 동기화 (정정: 이미 origin에 있었음)

사용자 지시: "방화벽 작업 끝나면 똑같은 UI 똑같은 기능을 써야 한다. 파인튜닝(SQL 정합성/튜닝)
부분은 그대로 유지." 처음엔 AIX 로컬 저장소의 git 이력(브랜치/스태시/reflog)만 확인하고 "매뉴통합"
반영본이 없다고 판단해 처음부터 새로 구현했는데, **이 판단이 틀렸다** — `origin`(GitHub)을
fetch하지 않고 로컬 이력만 봤던 것이 원인. 실제로는 사용자가 집에서 이미 작업해서 push까지 마친
상태였고, 그 버전이 이번에 새로 만든 것보다 더 발전돼 있었다(하이브리드 검색, 첨부문서, 타이핑
효과·자동 스크롤까지 포함).

AIX 쪽에서 로컬을 `origin/master`로 리셋해 기존 push본을 그대로 채택하고, 중복으로 새로 만들었던
버전은 버렸다. 그 위에 **origin에 아직 없던 진짜 신규 항목만** 다시 얹었다: Session Wait 색상/
체크박스 SID+SERIAL# 수정, 성능이력 필터 개선, `aidba.restapi.url` 통합, 그리고 origin의 SQL
작성기가 아직 "현재 계정 소유 테이블만 조회" 버전이라 OWNER 스키마+SYNONYM 자동탐색(①
`USER_TAB_COLUMNS` 우선 → ② `ALL_TAB_COLUMNS` 후보 탐색 → ③ 단일/복수 분기)을 추가 — GPU 호출은
origin이 이미 쓰던 `OllamaChatService.askWithPrompt()` 구조를 그대로 유지했다(별도 RestTemplate
서비스를 새로 만들 필요 없음). 상세는 AIX 저장소의 `작업정리_2026-09-14.md` 참고.

## 6. 코드 점검("지금 작업한 코드 점검 해줘")

`/code-review`로 5절 작업분을 점검, 5건 확인 후 양쪽 프로젝트 모두 수정:

1. Stored XSS: History 화면 계정/WAS서버 드롭다운 옵션을 이스케이프 없이 `innerHTML`로 조립 — 이스케이프
   헬퍼 추가.
2. Stored XSS: SQL 작성기 owner 선택 드롭다운도 동일 문제 — 수정.
3. 위 두 건 수정하는 함수 안에 있던 기존 계정 드롭다운도 같은 패턴이라 함께 수정.
4. `loadDbUsers()`가 계정/서버 목록 두 fetch를 순차로 `await`하던 것 → 동시에 시작해서 기다리도록
   변경(각자 try/catch는 유지).
5. `TableInfoService`(메인)의 `fetchColumns`/`fetchColumnsForOwner`,
   `fetchIndexes`/`fetchIndexesForOwner` 중복 로직을 공용 헬퍼로 통합(AIX는 처음부터 이 구조로
   작성해서 해당 없음).

## 7. GPU 서버 `sql_tuning_cases` 참조자료 mojibake 조사 (진행 중 → 원인 규명 완료)

"AI SQL 성능분석" 참조자료가 일부만 깨지는 현상 조사. 상세 원인·근거·제안 조치는
**[`next-tasks.md`의 "-1. GPU 서버 `sql_tuning_cases` 인덱스 중복/오염" 항목](next-tasks.md)** 참고 —
결론만: `OpenSearchIndexerService.indexData()`가 문서 색인 시 `_id`를 지정하지 않아 재실행마다
과거 색인분(그중 다수가 오염됨)이 지워지지 않고 누적된 것이 원인. 원본 `.hwp`는 컨테이너에 없음.
인덱스 삭제 후 현재 클린한 `tuning_data.jsonl`로 재색인하는 조치를 제안했고, **실행은 사용자 확인
대기 중**.

## 8. Current Session / Lock Holder-Waiter Tree 성능 문제 (사용자 보고 → 원인 규명 → 수정 완료)

사용자 보고: "세션 리스트 불러오는 게 엄청 오래 걸린다", "DB 접속이 느려진 것 같다", "Lock 세션
상세조회가 오늘 조건 추가해서 느려진 건가?".

**원인:** `MonitorService.queryLockWaitSidsByType()`(Current Session의 TX/TM Lock 집계, 5초마다
폴링)와 `getTmLocks()`(Lock Holder/Waiter Tree 탭)가 둘 다 `v$lock`을 무방비로 스캔한다. 같은
코드베이스의 `queryTxnPerMinute()` 주석에 이미 "이 환경 일부 인스턴스에서 v$lock 스캔 자체가
49초까지 걸림(enqueue 해시체인이 김)"이라고 실측 기록이 남아있었음(그래서 Fleet Overview는
v$sysmetric으로 갈아탐 — 근데 Current Session/Lock Tree 쪽은 안 고쳐져 있었음). 프론트에서는
`setInterval(fetchSessions, ...)`이 이전 호출 완료와 무관하게 계속 새 요청을 쏴서, v$lock이
느려지는 순간 요청이 겹쳐 쌓이며 오라클 커넥션 풀(기본 max 10)과 브라우저 연결 슬롯을 동시에
잠식 — 세션 리스트 지연/DB 접속 저하/SQL 실행 계정 로딩 지연(이건 실제로는 `databases.json`
메모리 조회라 자체 문제 아님, 연결 슬롯 경쟁의 부수효과)이 전부 이 하나의 원인으로 설명됨.
(2026-09-11에 추가된 `getSessionQuery()`의 `serial#` 조건 — 사용자가 처음 의심했던 지점 — 은
확인 결과 원인이 아니었음, v$session은 작은 인메모리 뷰라 조건 하나 추가로 몇 초씩 느려질 이유가
없음.)

**수정:**
- `queryLockWaitSidsByType()`/`getTmLocks()` 양쪽에 쿼리 타임아웃 추가 —
  `dbagent.monitor.lock-query-timeout-seconds`(기본 3초)로 외부화(`SqlQueryService` 등과 동일 관례).
- `app.js`: Current Session 자동 갱신 `setInterval` → 이전 `fetchSessions()` 완료 후에만 다음을
  예약하는 재귀 `setTimeout`으로 변경.
- `/code-review`로 검증 후 5건 추가 반영: (a) 위 fix 자체의 레이스 컨디션 — Start/Stop을 빠르게
  반복하면 폴링 체인이 두 개로 늘어날 수 있던 버그를 세대(generation) 카운터로 수정, (b) 타임아웃
  값 하드코딩을 설정으로 외부화, (c) 타임아웃/실패 시 부분 결과가 남던 것을 명시적으로 비움,
  (d) `getTmLocks()`가 원래 이 fix 대상에서 빠져있던 걸 발견해 추가 반영.
- **미해결로 남긴 캐비어트**: `Statement.setQueryTimeout()`이 실제로 쿼리를 끊으려면 오라클 드라이버의
  OOB(out-of-band) break가 네트워크상에서 동작해야 하는데, 방화벽/VPN 등이 이 경로를 막고 있으면
  타임아웃이 조용히 안 걸릴 수 있음 — 실제 환경에서 재발하면 로그의 `ORA-01013` 유무부터 확인.

---

## 빌드/배포

전 항목 `mvn clean package` → `dist`(메인 8006, AIX 8007) + `dist-aix`(폐쇄망 이관용, `.bak` 증가
+ `dist-aix.tar` 재생성) 3곳 모두 반영, 재기동 후 정상 기동 확인. git commit/push 완료
(main `a19fd92`→`0306ba9`→`5e77ee6`, AIX `c6a3261`→`c47a2b3`→`6be3de5`).
