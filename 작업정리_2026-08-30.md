# 2026-08-30 작업 정리

Fleet Overview(FO) 화면을 프로토타입에서 정식 화면으로 전환하고, 인스턴스 카드/Top 5에 DB 식별 정보(SID·버전)를 추가하고 가독성(폰트 크기/굵기)을 개선한 작업. 중간에 겪은 캐시 관련 장애와 근본 수정 내역도 함께 기록한다. `DBAgent-Java-AIX`(Java 8/Spring Boot 2.7 이관본)에도 전부 포팅·빌드·배포 완료 — 자세한 건 그쪽 리포지토리의 `작업정리_2026-08-30.md` 참고.

---

## 1. FO 화면 파일명 확정: `fleet-overview-test-blue.html` → `fleet-overview.html`

사용자 결정(2026-08-27)으로 이미 실제 로그인 후 랜딩 화면으로 쓰이고 있었지만 파일명이 "test"/"blue"(색상 비교용 프로토타입 시절 이름) 그대로 남아있어서, "이제 테스트가 아니니" 정식 이름으로 변경.

- `git mv`로 rename, 참조 전부 갱신: `app.js`(로그인 성공 리다이렉트 2곳 + FO 버튼 클릭 핸들러 + 주석), `index.html`(사이드바 로고 링크), `MonitorService.java`/`MonitorController.java`/`ConfigController.java`/`application.properties`(주석)
- 색상 비교용으로 남아있는 violet 버전(`fleet-overview-test.html`)은 그대로 유지, 헤더 주석만 새 파일명을 가리키도록 갱신

## 2. 인스턴스 카드/Top 5에 DB 식별 정보 추가

- **SID**: `/api/config`에 이미 있는 값이라 즉시 표시
- **Oracle 버전 코드네임**(21c/11g 등): `MonitorService.getFleetStatus()`가 `v$version.banner`("Oracle Database 21c Express Edition Release ...")에서 정규식(`Database\s+(\S+)`)으로 추출 - 메이저 버전→코드네임 매핑 테이블을 직접 관리할 필요가 없음. 실제 로컬 테스트 DB(Oracle XE 21, 컨테이너명 `oracle19c`)에 직접 쿼리해 정규식이 "21c"를 정확히 뽑는 것까지 확인
- 표시 포맷: `SID : ORCL[Oracle 21c]` (버전 미확인 시 `SID : ORCL`만)
- 카드(`db-meta`)와 Top 5 패널(`rank-meta`) 양쪽에 반영 - Top 5는 카드의 engine-icon이 없어서 "Oracle" 종류 표기를 버전 앞에 명시

## 3. 가독성 개선 (여러 차례 반복 조정)

요청이 세분화되면서 몇 차례에 걸쳐 조정했다 - 최종 상태만 요약:

- 인스턴스 카드: 최소 폭 300→340px, 패딩 18px 20px→20px 22px
- `Active Session`/`분당 트랜잭션`/`CPU`/`MEM`/`Uptime` **라벨**: 10.5~11px → 13px, weight 600 (처음엔 값 폰트로 착각해서 값 쪽을 키웠다가, "라벨이 작다"는 재확인 후 라벨 쪽을 별도로 키움)
- 위 항목 **값**: `stat-value` 16px/800, `mb-pct`(CPU/MEM %) 14px/800, `uptime-value` 22px/800 - 여러 차례 12.5px(SID/버전과 통일) ↔ 더 큰 값 사이를 오가다 "그래프가 커져도 상관없다"는 확인을 받고 최종적으로 키우는 쪽으로 정착
- CPU/MEM 미니바(그래프) 두께 5px→8px
- Uptime 도넛 링 56px→68px
- 주의 필요 Top 5: 패널 폭 320→360px, DB명 12.5→15px(bold), SID·버전 12.5px(bold) 신설, CPU% 13→16px(bold), 순위 원 20→24px
- 인스턴스 카드 DB 아이콘에 pulse 애니메이션 추가(2.4s 주기 scale 1↔1.15) - alive/degraded/로딩 중일 때만 움직이고 DOWN 상태는 정지 (`status-dot.pulse`와 같은 관례: 움직임 = 살아있음)

## 4. 오류 처리 기록 - 브라우저 캐시로 인한 "고쳤는데 안 바뀐다" 소동

배포 후 로그인하면 계속 옛 파일명(`fleet-overview-test-blue.html`)으로 리다이렉트되는 문제가 재현되어 원인 파악에 시간을 들였다.

1. **1차 오진**: dist/target 두 인스턴스가 동시에 포트 8006을 두고 기동되어 있던 걸 발견 - 이것도 실제 원인 중 하나였음(정리 후 재기동해도 증상 재현).
2. **진짜 원인**: `app.js?v=56`이라는 **정확히 같은 URL이 2026-08-25에 이미 다른 내용으로 쓰인 적**이 있어서, 브라우저 디스크 캐시가 그 옛 응답을 서버에 확인조차 안 하고 재사용(Ctrl+F5로도 재현) - Claude in Chrome으로 실제 브라우저에서 `fetch(url)`(기본 캐시)과 `fetch(url,{cache:'reload'})`(캐시 우회)의 응답을 직접 비교해서 확정했다.
3. **1차 대응**: `v=57`로 재부여(한 번도 안 쓰인 번호) → 해결되는 듯했으나, 캐시버스터가 없는 `fleet-overview.html` 자체도 같은 클래스의 문제를 겪음.
4. **근본 수정**: `com.dbagent.config.StaticResourceConfig`(`WebMvcConfigurer`) 신규 추가 - 모든 정적 리소스(`classpath:/static/**`)에 `Cache-Control: no-cache`를 강제해서, 브라우저가 매번 조건부 GET(If-Modified-Since)으로 재검증하도록 만듦. 파일이 실제로 바뀌었으면 반드시 새 응답을 받고, 안 바뀌었으면 여전히 304라 재다운로드 비용은 없음. `/api/*` 같은 `@RestController` 매핑은 이 리소스 핸들러보다 우선순위가 높아 영향받지 않는 것도 확인(`GET /api/login`→405, `GET /api/config`→200).

이 수정 이후로는 `?v=` 번호 재사용 충돌 문제가 재발하지 않을 것으로 기대됨. 자세한 진단 방법은 프로젝트 메모리(`build_environment.md`)에도 기록.

## 5. 검증

- `mvn clean package` 재빌드 → `dist\dbagent-java-0.1.0.jar` 교체(기존 `.bak` 백업) → `stop.ps1`/`start.ps1` 재기동, 매 변경마다 반복
- Claude in Chrome으로 실제 브라우저에서 캐시 우회/일반 요청 응답 비교, `Cache-Control` 헤더 확인, 카드/Top 5 레이아웃 스크린샷 확인(로그인 없이는 전부 DOWN 상태라 실제 alive 카드 스타일은 DOM에 가짜 데이터를 주입해 시각적으로 검증)
- `/api/login`(405), `/api/config`(200), `/api/fleet_status`(403, 토큰 없음) 라우팅이 새 정적 리소스 설정에 영향받지 않는 것 확인

## 6. 커밋 전 코드 리뷰(`/code-review high`)로 발견/수정한 버그

- **v$version 조회 실패가 전체 상태 조회를 실패시킴**: 버전 코드네임 조회 쿼리가 기존 로직과 같은 try 블록 안에 있어서, v$version에 대한 권한이 없는 계정 등으로 이 쿼리 하나만 실패해도 CPU/MEM/세션 등 나머지 지표까지 전부 "DOWN"으로 오분류될 위험이 있었음 → 별도 try/catch로 격리해서 이 조회가 실패해도 나머지 지표는 정상 반환되도록 수정.
- **DOWN 전환 시 옛 버전 정보가 카드에 남는 버그**: 인스턴스가 alive였다가 down으로 바뀌면, 백엔드의 down 응답에 `version`/`versionCodename` 키 자체가 없어서 프런트(`Object.assign`)가 예전 폴링에서 받은 값을 지우지 않고 그대로 유지 - "연결할 수 없습니다" 배너 옆에 옛 "[Oracle 21c]" 태그가 그대로 남아있는 것처럼 보임 → down 응답에도 두 필드를 명시적으로 `null`로 채워서 프런트 병합 시 확실히 지워지도록 수정.
- **(수정 안 함) v$version이 매 폴링(기본 3초)마다 재조회됨**: 버전은 인스턴스 재기동 전까지 안 바뀌는 값이라 캐싱하면 더 효율적이지만, 이 앱은 v$instance 상태/uptime 등 다른 "안 바뀌는" 값들도 원래부터 매 폴링마다 그냥 재조회하는 설계라 - 굳이 이 필드만 별도 캐시 상태를 두는 게 기존 설계 일관성에 안 맞다고 판단해 그대로 둠. 인스턴스 수가 매우 많아지면 재검토.

두 프로젝트 모두 재빌드/재배포/재기동 후 다시 헬스체크(`index.html`/`fleet-overview.html` 200) 완료.

## 7. `DBAgent-Java-AIX` 동기화

위 1~4 전부 포팅: 파일 rename, `MonitorService.getFleetStatus()`의 `version`/`versionCodename` 필드, `fleet-overview.html` 전체(diff 없이 동일 파일로 복사), `com.dbagent.config.StaticResourceConfig` 신규 추가, `app.js`/`index.html` 참조 갱신, 캐시버스터 57→58. `mvn clean package` 빌드 성공 확인(jar 안에 새 파일/클래스 포함), `dist`(Windows 테스트, 포트 8007) 재기동 후 브라우저로 main 프로젝트와 동일하게 렌더링되는 것 확인. `dist-aix`(실제 AIX 서버용) jar도 백업 후 교체했으나, 실제 AIX 서버에서만 구동 가능해 로컬에서 기동 검증은 불가 - jar 교체만 완료.
