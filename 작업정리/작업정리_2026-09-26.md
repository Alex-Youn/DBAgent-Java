# 작업정리 2026-09-26

## 1. F단계 — 대시보드 개편(체크리스트 9-2) 완료 · 배포 ⑤

전체 작업순서 F1~F9. main·AIX 동일 적용, 단계마다 로컬 XE 실세션 + CDP(헤드리스 Chrome) 검증.

| 단계 | 내용 | 커밋 (main / AIX) |
|---|---|---|
| F1 | 수집 테이블 5개(`mon_*`) + 보관 배치 | 7e3ea7b / 9924f43 |
| F2 | Lock 실시간 API + TM Holder KILL API(+활성 세션·메모리) | c851a4d / 575994a |
| F3 | Top·상세 드로어 API + SQL 통계 델타 | ea0c3b9 / 59e2537 |
| F4 | 프런트 뼈대(`nd-dashboard.js` 공통 엔진) | 2f86cc8 / 2e5ba7c |
| F4-1 | 보이지 않는 메뉴 조회 정지(오케스트레이터 결정) + 성능 이력 사용자 목록 DB 전환 버그 | 3ec3784 / 376690c |
| F5 | ① KPI · ② AAS 차트 · ④ 진단(`nd-aas.js`) | fc6f486 / 6f55df6 |
| F6 | ③ Lock 카드 · 장애 처리 KILL 화면(`nd-lock.js`) | 301135d / 9fe64d6 |
| F7 | ⑤⑥⑦ Top · 상세 드로어(`nd-top.js`) | 2af093c / a412542 |
| F8 | ⑧ 점검 알림(수집기·API·`nd-checks.js`) | 8449e1f / 5c23010 |
| 수정 | 성능 이력 Top 세션 `FETCH FIRST` → ROWNUM(11g ORA-00933) | b30bd35 / 950650f |

- 오케스트레이터 추가 요청 반영: ① KPI에 활성 세션·메모리 사용률 칸(숫자 아래 작은 막대, 기존 도넛 대체)
- 리뷰: security-reviewer(F2 KILL — Critical/High 없음, 감사 저장 재시도·예외 시 정리 반영), query-performance-reviewer(F3·F8 — SQL 텍스트 단건 조회, 드로어 1회 스캔, 잡 실패 30분 분리, 1일 점검 타임아웃 자동 제한, 관련 객체 캐시 등 반영)
- 11단계 추가 확인: 다중 탭 Lock 조회 주기당 1회(탭 3개·31초 11회), 라이트 테마, http 복사 대체(clipboard→execCommand→수동 안내), 접속 실패 표시, 수집 지연 배지
- 배포 ⑤ (2026-09-26 03:50): main dist·AIX dist·dist-aix, 기존 jar `.bak-20260926-0350`, 스모크(배포 jar) 통과·ERROR 0, 절차서 `가이드/폐쇄망_적용절차_2026-09-26_배포5.md`

## 2. 폐쇄망 확인 요청 (현장)

- 11g에서 새 대시보드 전체 동작, `/top` GROUPING SETS 1회 스캔 여부
- 1일 점검(테이블 용량 등)·SQL 통계 수집 소요 시간, 스케줄러 이력 테이블 크기
- 수집 계정 `dba_*` 권한(없으면 ⑧에 회색 "점검 실패" 카드), KILL용 `ALTER SYSTEM` 권한
- RAC 노드 등록 방식(`connect_mode=service` 여부)

## 3. 남은 결정·백로그

- 기존 `/api/kill_session`(Current Session·Lock Tree의 Kill)에 `canAccessDb`·재조회·USER 강제가 없음 — 새 KILL API 수준으로 맞출지 결정 필요(보안 리뷰 Low)
- ⑧ 관련 객체(테이블스페이스 top 세그먼트·테이블 인덱스)를 일 스냅샷으로 옮겨 클릭 시 원본 조회를 없애기(쿼리 리뷰 권고, 지금은 60초 캐시)
- 목업(`UI개선_mockup.html`)은 KPI 4칸 그대로 — 필요하면 6칸으로 갱신
- 로컬 XE 테스트 계정 `c##locktest`(테이블 t1~t8, 무효 뷰 v_bad, 실패 잡 JOB_FAIL_TEST) 남아 있음
- 다음 순서: G단계(세션 상세 리뉴얼·팝업 통일·성능 분석 개편)
