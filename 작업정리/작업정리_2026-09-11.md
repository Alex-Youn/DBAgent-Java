# 작업정리 2026-09-11

> 대상: `DBAgent-Java`/`DBAgent-Java-AIX`(AI DBA 챗봇, SQL Tune Advisor) + 신규 `sqlrestapi`(Docker
> `rocky9-server` 컨테이너, RAG 서버, 폐쇄망 GPU 서버 이관 대상)
> 상태: 로컬 테스트 완료, sqlrestapi 코드 정밀검사(버그 8건 수정) + 폐쇄망 이관 패키징까지 완료.
> DBAgent-Java/AIX 쪽은 **미커밋**.

---

## 1. 세션 상세 SQL 불일치 버그 수정 (next-tasks.md 0.1~0.4)

- `MonitorService.getSessionQuery()`가 `serial#`을 받지 않아 SID 재사용 시 엉뚱한 세션의 SQL을
  보여주는 문제 → `serial` 파라미터 추가, v\$session 조회에 serial# 매칭 반영.
- 클라이언트가 이미 알고 있는 `sql_id`를 서버가 재조회해서 덮어쓰던 부분 제거 — 클라이언트 값을
  신뢰하고, v\$sql `child_number` fallback만 추가.
- `session-list.html`/`session-detail.html`/`app.js`: `data-serial` 속성 추가, URL로 전달, 전역 클릭
  델리게이트 포함 6곳 수정.
- **부가로 발견**: 드래그 선택(brush) 중에도 실시간 차트 갱신이 계속 일어나 좌표가 튀는 문제 —
  `window.scatterDragActive` 플래그로 드래그 중엔 차트 갱신을 얼려서 해결(0.2절).
- 두 저장소(DBAgent-Java/AIX) 동일 반영.

---

## 2. sqlrestapi — RAG 서버를 Docker(rocky9-server)에 구축

기존에 집에서 설계해 둔 `자바 프로젝트와 ollama 연동구조.md`를 바탕으로, 회사 PC의 Docker Rocky 리눅스
컨테이너 안에 이미 뼈대만 있던 `/SqltuneRestApi/app/sqlrestapi`를 실제로 동작하게 만드는 작업.

### 2-1. 집→회사 PC 이관으로 생긴 경로 문제

tar로 통째로 백업해 회사 PC 컨테이너에 풀었더니 모든 스크립트/설정에 **집 PC 기준 경로**가 그대로
박혀 있었다. 전수 조사해서 수정:

- 안 쓰는 JDK17 삭제, 실제 쓰는 JDK17을 `sqlrestapi/jdk17`로 재배치, 참조 경로 전부 갱신.
- `start_server.sh`/`stop_server.sh`/`build.sh`/`run_indexer.sh`/`start_opensearch.sh`/
  `stop_opensearch.sh` 전부 경로 수정 (→ 이 절의 수정도 사실 **11절에서 다시 갈아엎힘**, 아래 참고).
- REST API 포트 8080 → **9300**으로 변경 (방화벽 신청을 9300 하나만 할 예정이라).

### 2-2. Ollama 설치 (회사 PC는 GPU 없음, CPU 전용, 메모리도 넉넉지 않음)

- `bge-m3`(임베딩) + `qwen2.5-coder:3b`(생성, 실제 폐쇄망 목표 모델 `qwen3-coder:30b`는 회사 PC에서
  돌릴 수 없어 축소 테스트용) 설치.
- 여유 확보를 위해 로컬 Oracle 19c 컨테이너는 내림.
- `OLLAMA_MAX_LOADED_MODELS=1`, `OLLAMA_KEEP_ALIVE=5m` — 모델 하나만 물고 있게 제한.

### 2-3. 겪은 환경 문제들

| 문제 | 원인 / 조치 |
|---|---|
| MSYS_NO_PATHCONV | Git Bash가 `/`로 시작하는 인자를 윈도우 경로로 오인 변환 — `docker` 계열 명령에서 반복 발생, `MSYS_NO_PATHCONV=1` 접두로 회피 (여러 번 재발, 11절 참고) |
| KIPOSSL 사내 SSL 가로채기 | 대부분의 외부 사이트가 자체서명 인증서 오류 — 루트 CA 추출해 `update-ca-trust`로 신뢰 등록 |
| OpenSearch 포트 충돌 | transport 기본 포트 9300이 sqlrestapi 포트와 겹침 — `transport.port: 9400`으로 이동 |
| OOM (긴 임베딩 입력) | OpenSearch 힙 512m → 256m로 축소 |
| `docker commit` 후 Lucene lock 오염 | 떠 있는 컨테이너를 commit하면서 lock 파일 상태가 불일치 — `*.lock` 삭제 후 클린 재기동 |
| OpenSearch 프로세스 중복 기동 | `ps` 명령이 컨테이너에 없어 "이미 실행 중" 체크가 무력화됨 — 전부 kill 후 1회만 기동 |

---

## 3. SQL Tune Advisor 메뉴 (신규)

AI DBA 메뉴 바로 아래 추가. 자유 텍스트 질문 → `com.dbagent.sqltuneadvisor`(Controller/Service, 신규
패키지) → sqlrestapi의 `RAGController`(`/query`, `/query/stream`) 호출 → `{answer, references}` 렌더링.
스트리밍 지원(`ResponseBodyEmitter` + `HttpResponse.BodyHandlers.ofLines()`). 관리자 제한 없음(AI DBA
챗봇과 동일 성격). 두 저장소 동일 반영, 인덱싱까지 확인 후 `restapitest.sql`로 실제 테스트 완료.

---

## 4. AI DBA 챗봇 → sqlrestapi 프록시로 전환 + 스트리밍 UI

방화벽을 9300 하나만 신청할 예정이라, 두 프로젝트 모두 **Ollama(11434)에 직접 붙지 않고 sqlrestapi를
거치도록** 전면 수정.

- **sqlrestapi에 범용 `/api/chat` 신설** — 사내 튜닝 사례 검색 없이 프롬프트+컨텍스트만 받아 Ollama에
  넘기고 답변만 돌려주는 단순 프록시.
- `OllamaChatService`(양쪽 프로젝트) 재작성 — 직접 Ollama 호출 제거, sqlrestapi의 `/api/chat`
  (`ask()`)과 `/api/query`(`askWithSemanticSearch()`, 하이브리드 폴백용) 호출로 전환.
- **토큰 단위 스트리밍 UI** — `app.js`의 `run()`이 `fetch` + `ReadableStream`으로 한 글자씩 렌더링,
  결과 출력창이 내용 크기에 맞춰 커지도록 처리.
- AIX 쪽도 동일 반영(RestTemplate 기반, Java 8 문법: `Strings.isBlank()`, `Maps.of()`).
- 양쪽 다 실제 기동해서 검증 완료(8006/8007).

---

## 5. 멀티 프롬프트 구조 + AI DBA 챗봇 하이브리드 검색

### 5-1. 멀티 프롬프트

- sqlrestapi에 `PromptService` 신설 — `prompts/<promptId>.md` 파일 기반, **파일 mtime 확인해서 바뀐
  경우만 재로딩** → 프롬프트 내용만 바꿀 땐 **재기동 불필요**(기존 `System Prompt.md` 단일 파일은
  생성자에서 한 번만 읽어 캐싱하던 방식과 다른 점).
- `prompts/tuning.md`(SQL Tune Advisor), `prompts/chatbot.md`(AI DBA 챗봇, 기존 하드코딩 페르소나를
  옮김).
- `RAGController`의 `/api/chat`, `/api/query`, `/api/query/stream` 전부 `promptId`(+`index`)를 받는
  형태로 일반화. 기본값(`promptId=tuning`, `index=sql_tuning_cases`)이 있어 SQL Tune Advisor는 코드
  변경 없이 그대로 동작(회귀 확인 완료).

### 5-2. AI DBA 챗봇 하이브리드 검색

- `ErrorSearchService`의 ORA 코드 정확 일치/키워드(SQLite LIKE, `data/oracle_errors.db`)가 뭔가
  찾으면 **빠른 경로**(`/api/chat`, 검색 없음)로 바로 답변.
- 못 찾으면(자연어 질문 등) `askWithSemanticSearch()`가 sqlrestapi의 `/api/query`를
  `promptId=chatbot, index=error_dictionary`로 호출해 OpenSearch 시맨틱 검색 + 생성을 한 번에 받음.
- 이 시점엔 `error_dictionary` 인덱스에 **300건만** 색인된 상태였음(전체 9,372건 중 알파벳/코드순
  앞부분) — **9절에서 나머지 마저 색인**.
- 테스트 중 발견: 작은 모델(`qwen2.5-coder:3b`)이 애매한 질문에서 **반복 루프**(13,000+ 토큰)에 빠지는
  현상 — `num_predict`/repetition penalty 미설정이 원인으로 추정, **미수정**(다음에 이 주제 나오면
  검토).

---

## 6. sqlrestapi 코드 정밀검사 — 버그 8건 발견/수정

사용자 요청("코드 정밀검사 수행해줘, 버그 있으면 수정해줘")으로 sqlrestapi 전 소스 파일을 훑음.

| # | 위치 | 문제 | 수정 |
|---|---|---|---|
| 1 | `PromptService.getPrompt()` | `promptId` 검증 없이 파일 경로에 그대로 사용 → path traversal | `SAFE_ID` 정규식(`[a-zA-Z0-9_-]+`) 검증, 불일치 시 기본 프롬프트 폴백 |
| 2 | `OpenSearchSearchService.getEmbedding()` | Ollama 호출 실패 시 예외가 그대로 전파 | try/catch로 안전 처리 |
| 3 | `OpenSearchIndexerService.getEmbedding()` | 동일 | 동일 |
| 4 | `OpenSearchIndexerService.indexData()` | try/catch가 while 루프 **밖**에 있어 문서 1건 실패 시 배치 전체 중단 (**실측 사고**: 1301건 중 1248건에서 멈춤) | try/catch를 루프 **안**으로 이동, 문서별 실패 격리 |
| 5 | `RAGController` `/api/query`, `/api/query/stream` | 빈 질의 검증 없음 → NPE 가능 | blank 체크 추가 |
| 6 | `RAGController` `n_results` | null 명시 시 언박싱 NPE | null-safe 헬퍼 |
| 7 | `TestRunner`, `ErrorDictIndexerRunner` | 하드코딩 절대경로 | 상대경로로 변경 |
| 8 | `OllamaService.generate()` / `generateStream()` | (a) 응답에 `response` 키 없으면 `null` 반환 → `Map.of("answer", answer)`에서 NPE. (b) Ollama 호출 자체 실패(연결 끊김 등) 시 예외 처리 없음 | `generate()`는 null 대신 `""` + try/catch로 안내 메시지 반환. `generateStream()`은 `.onErrorResume()`으로 스트림 안 깨고 에러 안내 라인 한 줄 흘려보낸 뒤 정상 종료 |

빌드·재기동·curl 검증(path traversal, blank query, `n_results:null`, 정상 chat 호출) 전부 통과.

**추가로**: `ChromaDBService.java`(어디서도 참조 안 되는 죽은 코드, OpenSearch 도입 전 ChromaDB 실험
잔재)를 발견해 별도로 요청받아 삭제.

---

## 7. 폐쇄망 이관 패키징 (rga.tar)

`tar -cvf` → `docker cp`로 로컬 `C:\AI-PROJECTS\RestApiServer\rga.tar`로 반출하는 작업을 여러 차례
반복(코드/설정이 계속 바뀌었으므로).

### 7-1. tar 전에 반드시 서비스 클린 셧다운

`docker commit`으로 인한 Lucene lock 오염 전례(2-3절) 때문에, tar 뜨기 전엔 항상
`stop_server.sh`/`stop_opensearch.sh`로 정상 종료 후 진행. (lock 파일 자체는 클린 셧다운 후에도
남는 게 정상 — 다음 기동 때 재획득됨.)

### 7-2. Ollama 디렉토리는 tar에서 제외

처음엔 `/SqltuneRestApi/app` 전체를 담아 **3.7GB**가 나왔는데, 사용자가 "생각보다 큰데 ollama 들어
있어?"라고 물어봐서 확인 → `ollama/lib`(런타임, 2.1GB)이 포함돼 있었음. 단, **모델 가중치(2.9GB,
`/root/.ollama/blobs`+`manifests`)는 애초에 `/SqltuneRestApi/app` 밖이라 포함 안 됨**. 폐쇄망 서버에
이미 Ollama+모델이 올라가 있다고 확인받아 `--exclude='SqltuneRestApi/app/ollama*'`로 재생성 →
**1.6~1.7GB**로 축소.

### 7-3. 모델명 설정

`application.yml`의 `sqltune.llm.model`을 폐쇄망 목표 모델 `qwen3-coder:30b`로 변경 → 재빌드 →
재tar. 이때 로컬 테스트박스가 그 모델을 안 갖고 있다는 걸 재기동 후에야 알아차림(재기동은 최종
스냅샷을 위해 어쩔 수 없이 필요했음) — **8절**에서 근본 해결.

---

## 8. 나머지 error_dictionary 9,072건 색인 + 도구 두 종 신설

색인 자동화 도구가 없어서(예전엔 1회성 인라인 `python -c`로 처리) 재사용 가능한 스크립트 2개 신설.

- **`export_error_dictionary_to_jsonl.py`**(`DBAgent-Java` 루트) — `data/oracle_errors.db`
  → JSONL. 기존 `error_dictionary_full.jsonl`과 동일 스키마(`_filename`/`error_code`/`cause`/`action`).
- **`sqlrestapi/run_indexer_errors.sh`** — `error_dictionary.jsonl` → OpenSearch `error_dictionary`
  인덱스 (`profile=indexer-errors`, 포트 8082로 기존 튜닝-사례 인덱서와 충돌 회피).

색인 전 **기존 300건 인덱스를 삭제**(재색인 시 새 문서로 계속 추가되는 구조라 안 지우면 중복 발생).
9,372건 전체 색인 — **소요 35분 59초, 실패 0건**(CPU 전용 bge-m3 임베딩). 완료 후
`data/users/`... 아님 그냥 `/error_dictionary/_count`로 9,372 확인.

색인 완료 후 클린 셧다운 → tar 재생성 → 로컬 반출.

---

## 9. Ollama 모델/URL 설정 외부화

기존엔 `sqltune.llm.model`만 `application.yml`로 뺐고, Ollama URL(`http://localhost:11434`)은
`OllamaService`, `OpenSearchSearchService`, `OpenSearchIndexerService` **세 곳 모두 하드코딩**돼
있었다(질문 계기로 발견). `sqltune.llm.url` 프로퍼티 신설, 세 서비스 생성자에
`@Value("${sqltune.llm.url:http://localhost:11434}")` 주입으로 통일.

### 9-1. 재빌드 없이 모델 바꾸는 법도 정리

Spring Boot `@Value` 프로퍼티라 **환경변수/커맨드라인 인자로 재빌드 없이 override 가능**
(`SQLTUNE_LLM_MODEL`, `SQLTUNE_LLM_URL` 또는 `--sqltune.llm.model=...`). 이걸 활용해
**`start_server_prod.sh`** 신설 — 폐쇄망 전용 기동 스크립트, env var로 `qwen3-coder:30b`+URL만
override하고 기존 `start_server.sh`에 위임. jar/`application.yml` 기본값은 로컬 테스트용
(`qwen2.5-coder:3b`)으로 그대로 둘 수 있어, **이후로는 모델 하나 바꾸자고 tar를 통째로 다시 만들 일이
없어짐.**

로컬에서 `start_server_prod.sh` 실행 → health에 `qwen3-coder:30b` 반영 확인 → 다시
`start_server.sh`로 복귀해 로컬 테스트용 상태 복구, 검증 완료.

`rebuild_and_restart.sh`도 신설(build → stop → start → health check 한 번에, 이번 세션에서 수동으로
반복하던 패턴을 스크립트화).

---

## 10. 배포 스크립트 경로 독립화 ⚠️ 중요

폐쇄망 서버에 풀 때 "**상위 디렉토리명이 달라도 되는지**" 질문이 계기.

**답은 원래 "안 됨"이었다** — `start_opensearch.sh`, `stop_opensearch.sh`, `build.sh`,
`run_indexer.sh`, `run_indexer_errors.sh`, `stop_server.sh`, `start_server.sh`,
`rebuild_and_restart.sh` **8개 전부**가 `/SqltuneRestApi/app/...` 절대경로를 문자 그대로 하드코딩하고
있었다(2-1절에서 "고쳤다"고 했던 것도 사실 **집 PC 경로 → 회사 PC의 같은 절대경로**로 바꾼 것뿐,
경로 자체를 상대화한 게 아니었음). `/opt/foo/app`처럼 다른 위치에 풀면 전부 깨짐.

**수정**: 8개 스크립트 전부 `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`로 자기 위치
기준 상대경로화. (`start_server_prod.sh`는 이미 `$(dirname "$0")` 패턴이라 손 안 댐 — 처음부터 옳게
짰던 셈.)

**검증**: `/tmp` 등 무관한 cwd에서 절대경로로 각 스크립트 실행 → 정상 기동, health/색인 건수(9,372)
유지 확인. 이제 tar를 어디에 풀든(경로 앞부분이 달라도) 동작한다.

### 10-1. 재발한 MSYS_NO_PATHCONV 함정

검증 중 `docker exec ... bash -c "/SqltuneRestApi/app/start_opensearch.sh ..."`처럼 **`-c` 인자
문자열이 `/`로 시작**하면 Git Bash가 그 안의 경로까지 윈도우 경로(`C:/Program Files/Git/...`)로
잘못 변환해버리는 현상을 다시 겪음(`cd /path && ...`처럼 `/`로 시작하지 않게 감싸면 안 겪던 것과
대조). `MSYS_NO_PATHCONV=1` 접두로 해결 — **기존 메모리("docker 명령에서 발생")보다 조건이 더
좁다는 걸 이번에 확인**: `-c` 문자열 자체가 `/`로 시작할 때 발생.

---

## 11. 최종 상태

- `C:\AI-PROJECTS\RestApiServer\rga.tar` (~1.7GB) — 최신 코드(버그 수정 8건 반영), 경로 독립화 완료,
  error_dictionary 9,372건 전체 색인 완료, 로컬용 기본 모델(`qwen2.5-coder:3b`) 상태로 패키징.
  사용자가 이미 tar를 밖으로(물리 반출용으로 추정) 빼 감.
- 로컬 테스트박스(`rocky9-server`)는 정상 상태로 복구되어 계속 테스트 가능.
- 폐쇄망 서버에서는 `./start_server_prod.sh`로 기동(재빌드/재색인 불필요, `qwen3-coder:30b` +
  로컬 Ollama 그대로 사용).

## 12. 남은 사항

> ⚠️ 이 절은 **낮 시간대 기준**이다. 저녁 세션에서 일부 해소되었으므로 현재 상태는 **23절**을 볼 것.

- `num_predict`/repetition penalty 미설정으로 인한 반복 루프 현상 — 미수정.
- SQL 작성/생성 기능을 SQL Tune Advisor에서 활용할지, 트레이스 연동 UI 배치(① SQL 정합성/튜닝 화면에
  버튼 추가 vs ② SQL Tune Advisor 화면에 독립 컨트롤 복제) — 미결정, `sql_tune_advisor_feature.md`
  메모리 참고.
- AIX 쪽은 LIKE 키워드 폴백이 원래 없어(ORA 코드 정확 일치만) 하이브리드 "못 찾으면" 조건이 원본보다
  더 자주 걸림 — LIKE 폴백을 AIX에 새로 추가하진 않음, 필요하면 별도 논의.
- DBAgent-Java/AIX 쪽 오늘 변경분(세션 상세 버그 수정, SQL Tune Advisor, 챗봇 프록시 전환, 하이브리드
  검색) **미커밋**.

---

# (이어서) 저녁 세션 — 2026-09-11 20:00 ~ 09-12 새벽

> 위 1~12절은 낮 시간대 작업(집에서 push한 내용 포함). 이 아래는 회사 PC에서 그것을 받아
> 배포하고 이어서 진행한 작업이다.

---

## 13. 양쪽 저장소 pull — 로컬 변경 때문에 막힘

`git pull`이 "Your local changes would be overwritten by merge"로 중단됐다. 원인은 **추적 중인
런타임 파일**(`dist/users.db`, `dist/dbagent-java.out.log`, `.pid`)과 target 빌드 산출물이
계속 변경 상태였기 때문. 매번 겪던 문제라 이번에 **22절에서 근본 정리**했다.

처리 순서: 설정/DB 파일을 `backup_pull_2026-09-11/`로 백업 → 충돌 파일 checkout → pull →
로컬 값 복구 → skip-worktree 재적용.

| 저장소 | 받은 커밋 |
|---|---|
| `DBAgent-Java` | `211ce32..9cc1276` (3개) |
| `DBAgent-Java-AIX` | `10c16da..9c9f8d3` (4개) |

**AIX 쪽은 이미 포팅이 되어 있었다.** 원격의 `9c9f8d3`이 "AI DBA 챗봇 sqlrestapi 프록시 전환 +
SQL Tune Advisor 메뉴 + 세션 상세정보 버그 수정"으로 낮에 원본에 한 작업과 동일 내용이었다.
별도 포팅 작업 불필요.

### 13-1. pull이 TNS 경로를 깨뜨릴 뻔함

원격 `application.properties`의 `dbagent.oracle.tns-admin`이 `C:/Oracle/docker/network/Admin`인데
**이 PC에는 그 경로가 없다**(실제는 `C:/oracle_docker_data`). 그대로 받았으면 Oracle 접속이 전부
실패했을 것. pull 후 로컬 값으로 되돌렸다. 설정 파일이 skip-worktree라도 **원격에서 바뀌면 pull이
덮어쓴다**는 점에 주의.

### 13-2. `dist/databases.json`이 새로 생겼다

원래 로컬 dist에 없던 파일인데 원격 커밋에 포함돼 내려왔고, 루트 `databases.json`과 내용이 다르다
(Oracle sid `ORCL3/4/5` → `SRCH1/SRCH2/PORT1`, 계정 `dbagent_user` → `system`, `kipoadm` 추가).
"엉뚱한 DB 접속" 패턴에 해당하므로 dist 실서비스 기동 전 확인 필요. **미처리 상태.**

---

## 14. 빌드/배포 3곳

`mvn clean package`(pull 직후는 항상 `clean` — 9월 4일 stale 리소스 사고) 후 배포.

| 대상 | 포트 | 처리 |
|---|---|---|
| `DBAgent-Java/dist` | 8006 | jar 교체 + 재기동 |
| `DBAgent-Java-AIX/dist` | 8007 | jar 교체 + 재기동 |
| `DBAgent-Java-AIX/dist-aix` | — | jar만 교체(폐쇄망 반출용, 로컬 기동 검증 생략) |

교체 전 기존 jar은 `.bak-<날짜시각>`으로 백업. 배포 후 실제 서빙 콘텐츠를 curl로 확인
(SQL Tune Advisor 반영 여부) — "기동 성공" 로그를 믿지 않는다는 원칙 그대로.

---

## 15. `application.properties.sample` 현행화

코드의 `@Value` 키를 전수 grep해서 샘플 파일과 1:1 대조했다.

- **양쪽 공통**: `sqltuneadvisor.api.url` / `.timeout-ms` 2개가 누락되어 있어 추가
- **AIX만**: AI DBA 항목이 **아직 Ollama 직결 시절 내용**(11434 직접 접속, `aidba.ollama.model`
  지정)을 설명하고 있었다. `aidba.ollama.model`은 4절에서 코드에서 제거된 프로퍼티인데 샘플에는
  남아 있던 것 — sqlrestapi 프록시 방식으로 갱신하고 삭제했다.

대조 결과 두 프로젝트 모두 **누락·잉여 없이 일치**. jar 내장
`src/main/resources/application.properties`는 이미 전체가 주석과 함께 갖춰져 있어 손대지 않았다.

---

## 16. `rga.tar` 컨테이너 반입

`C:\AI-Projects\SqlRestApi\rga.tar`(1.8GB) → `rockylinux9` 컨테이너. SHA-256 일치 확인.
(7절에서 반출했던 것을 다시 넣은 것)

---

## 17. 컨테이너 GPU 패스스루 + Ollama 기동 체계 신설

### 17-1. GPU가 안 붙어 있었다

`rockylinux9` 컨테이너가 `--gpus` 옵션 없이 생성되어 **CPU 전용**이었다(호스트에는 RTX 4070 SUPER
12GB가 있는데). 2-2절에서 "회사 PC는 GPU 없음"이라고 적었던 것은 **컨테이너 설정 문제**였던 셈.

GPU 옵션은 컨테이너 생성 시에만 지정 가능해 재생성이 필요했고, **볼륨이 하나도 없어서** 그냥
지우면 `/SqltuneRestApi`와 `/root/.ollama`(모델 19GB)가 전부 날아가는 상황이었다.

처리: `docker commit rockylinux9 rockylinux9_rag:backup-20260911`(56.5GB)로 백업 → `docker rm` →
`docker run --gpus all` + 동일 포트(11434/8080/9200/9300)로 재생성 → 데이터 보존 확인.

### 17-2. 모델은 이미 다 있었다

`ollama`도 설치되어 있었고(`/usr/local/bin/ollama`, v0.33.3), `bge-m3`(1.2GB)와
`qwen3-coder:30b`(18GB)도 이미 받아져 있었다. 재설치·재다운로드 불필요.

**GPU 성능**: 18GB 모델을 12GB VRAM에 다 못 올려 부분 오프로드로 도는데 **약 19 tok/s**
(30B-A3B MoE라 활성 파라미터가 적어서 나오는 속도). 다만 **콜드스타트 모델 로딩에 30초 이상**
걸린다 — 19절 타임아웃 문제의 원인.

### 17-3. 기동/종료 스크립트 신설

`start_opensearch.sh`/`stop_opensearch.sh`와 같은 PID 파일 방식으로 작성.

- `/SqltuneRestApi/app/start_ollama.sh` — `OLLAMA_HOST=0.0.0.0:11434`로 기동, 로그/PID 파일 생성,
  이미 떠 있으면 스킵
- `/SqltuneRestApi/app/stop_ollama.sh` — PID로 종료, 없으면 `pkill -f 'ollama serve'` 폴백

---

## 18. OpenSearch / sqlrestapi 기동 — 겪은 문제 2건

| 문제 | 원인 / 조치 |
|---|---|
| `can not run opensearch as root` | 재생성한 컨테이너의 기본 사용자가 root. **`docker exec -u ai_user`** 로 실행해야 함 |
| `AlreadyClosedException: Underlying file changed by an external force` | **17-1의 `docker commit`이 Lucene lock을 오염**시킴 (2-3절에 기록된 것과 동일 재발). `Opensearch/data` 밑 `*.lock` 6개 삭제 후 클린 기동으로 해결, 데이터 손실 없음 |

기동 후 확인: `error_dictionary` 9,372건, `sql_tuning_cases` 3,094건 온전.
sqlrestapi는 `start_server_prod.sh`로 기동 →
`{"llm_model":"qwen3-coder:30b","embedding_model":"bge-m3","status":"ok","vector_db":"connected"}`

> **lock 오염은 "로키 리눅스라서"가 아니라 "라이브 컨테이너를 commit해서" 나는 문제다.**
> 폐쇄망 이관은 tar 반출 방식이라 해당 없지만, 거기서도 백업 목적으로 라이브 commit을 하면 똑같이 난다.

---

## 19. AI DBA 챗봇 `Read timed out` — 어느 파라미터인가

사용자가 겪은 오류:

```
sqlrestapi(http://localhost:9300)에 연결할 수 없습니다: I/O error on POST request for
"http://localhost:9300/api/chat": Read timed out
```

**에러 문구로 AIX 쪽임을 특정했다.** 이 메시지 형식(`ResourceAccessException` + RestTemplate)은
`DBAgent-Java-AIX`의 `OllamaChatService`에만 있다. 원본은 `java.net.http.HttpClient`를 써서
예외 형태가 다르다.

- **파라미터**: `aidba.ollama.timeout-ms` (AIX는 `SimpleClientHttpRequestFactory`의 connect/read
  타임아웃에 같은 값을 쓴다)
- **원인**: AIX jar 기본값이 **30000(30초)** 인데 `qwen3-coder:30b` 콜드스타트가 이를 넘김(17-2)
- **조치**: AIX의 `application.properties` / `dist/application.properties`에 `120000` 추가 후 재기동
- 원본 `DBAgent-Java`는 기본값이 300000(5분)이라 해당 없음 — 확인 후 손대지 않았다

> 이 파일들은 skip-worktree라 **git으로 전파되지 않는다.** 회사/폐쇄망에서 같은 증상이 나면
> 거기서 직접 넣어야 한다.

---

## 20. SQL 정합성/튜닝 — 분석 버튼 2개 삭제

사용자 요청으로 **"실행계획 조회 후 분석"(EXPLAIN PLAN)** 과 **"실제 실행 통계로 분석"
(DISPLAY_CURSOR)** 버튼 제거.

- 두 버튼 전용이던 `runSqlTuningAutoMode()` 헬퍼와 리스너, DOM 참조까지 정리(죽은 코드 미잔류)
- **"1차 성능점검" 버튼과 바인드 변수 패널은 유지** — 1차점검도 쿼리를 실제 실행하므로 바인드가 필요
- 바인드 패널 안내 문구가 삭제된 버튼을 가리키고 있어 "1차 성능점검"으로 갱신
- 백엔드 API(`analyze_from_query`, `analyze_from_query_actual`)는 요청 범위 밖이라 유지
- 캐시버스터 `app.js?v=100 → v=101` (양쪽 저장소 git 이력 전체를 훑어 **한 번도 쓰인 적 없는 번호**
  인지 확인 후 사용 — 8월 29일 캐시 충돌 사고 재발 방지)

양쪽 빌드 → 3곳 배포 → 실제 서빙 HTML에서 버튼 제거 확인.

---

## 21. AI DBA 메뉴 통합 — 설계 확정 (구현은 다음 세션)

"SQL Tune Advisor가 AI DBA와 성격이 비슷한데 합치는 게 어떤가"에서 시작해, **설계만 확정하고
구현은 하지 않기로** 했다. 확정안은 **`매뉴통합.md`** 에 정리(8개 절).

핵심: `SQL Tune Advisor` 최상위 메뉴를 없애고 AI DBA 안으로 흡수. **좌측 프레임**(AI SQL Tunner /
AI 챗봇 / AI SQL 작성기 / Regex 오류검색) + **AI SQL Tunner 하위에만 탭 2개**. 진입 기본 화면은
AI SQL Tunner. 신규 기능 2개(**AI Current SQL 분석**, **AI SQL 작성기**). 3단계로 진행하며
각 단계마다 메인 → AIX 순.

### 검토 과정에서 확인한 사실들

- **화면 간 데이터 전달 메커니즘이 이미 있다.** 세션상세 팝업의 `Tuning` 버튼이
  `window.opener.openSqlTuningFromPopup(...)`으로 메인 화면 입력창을 채운다(`app.js:3198`).
  새로 설계할 필요 없이 목적지만 바꾸면 된다. 단 현재 **Plan을 안 넘기므로** 확장 필요.
- **브라우저에서 sqlrestapi 직접 호출이 가능하다.** 방화벽 9300을 열 예정이고,
  `RAGController`에 `@CrossOrigin(origins = "*")`가 이미 있어 CORS도 막히지 않는다.
- **`/health`의 `llm_model`은 신뢰할 수 없다.** 설정값을 echo할 뿐 Ollama를 확인하지 않아
  Ollama가 죽어도 같은 값이 나온다 → **모델은 이름만 표시하고 연결 상태등은 넣지 않기로 결정.**
  반면 `vector_db`(OpenSearch)는 실제 확인값이라 살린다.
- **`isConnected()`에 타임아웃이 없다**(`.block()` 무제한). OpenSearch가 "응답 없음" 상태면
  `/health` 자체가 멈춘다 → sqlrestapi 개선 후보.
- 실제 실행 경로는 **SELECT/WITH만 허용**(`ExecutionPlanService:98`). AI SQL 작성기의 실행 연동도
  이 제약을 따른다.

### 시스템 프롬프트 초안 2종 (`prompts-draft/`)

현재 `prompts/`에는 `tuning.md`, `chatbot.md` 2개뿐이라 2개를 더 만들어야 한다.

- **`current-sql.md`** — 실측 데이터 해석 전용. **E-Rows(예상) vs A-Rows(실제) 괴리 지점을
  계획 라인 Id로 지목**하는 것이 핵심. 사내 사례 RAG를 안 타므로 `tuning.md`를 복사해 고치면 안 된다.
  "실측치상 문제없으면 문제없다고 말하라"를 명시해 없는 개선점을 지어내는 것을 막았다.
- **`sql-writer.md`** — 최대 실패 모드인 **컬럼명 환각**(`CREATE_DATE`, `USE_YN` 류를 추측해 넣어
  `ORA-00904`) 방지가 핵심. SELECT/WITH 전용, 코드블록 1개 제한(실행 자동화용).

> 초안은 아직 **컨테이너에 미배포**. `prompts/`로 복사하면 되고, `PromptService`가 mtime 핫리로드라
> 재기동은 불필요하다.

---

## 22. 커밋/푸시 + 런타임 파일 추적 해제

### 22-1. 커밋 4건

| 저장소 | 커밋 | 내용 |
|---|---|---|
| `DBAgent-Java` | `cef4bd5` | 버튼 2개 제거 + `매뉴통합.md` + `prompts-draft/` + sample 보완 |
| `DBAgent-Java-AIX` | `6e0475f` | 버튼 2개 제거 + sample 갱신 |
| `DBAgent-Java` | `f5ed3ea` | 런타임 파일 추적 해제 |
| `DBAgent-Java-AIX` | `3b979bb` | 런타임 파일 추적 해제 |

설정 파일(비밀번호 포함)은 skip-worktree로 자동 제외됐고, 백업 폴더(`backup_pull_2026-09-11/` —
안에 설정 파일 사본이 있음)도 제외되었음을 커밋 전에 확인했다.

### 22-2. 런타임 파일 추적 해제 (13절 문제의 근본 정리)

`users.db`/`users.mv.db`, out·err 로그, `run.log`, `dist/dbagent-java.pid`를
`git rm --cached` + `.gitignore` 처리.

**`.gitignore` 패턴은 경로를 하나씩 좁게 지정했다.** `*.db`처럼 넓게 잡으면
`data/oracle_errors.db`(에러사전 9,372건, 정상 데이터)까지 걸려서 8월 27일 gitignore 사고와
같은 부류가 된다. `git check-ignore -v`로 에러사전이 무시 대상이 **아님**을 검증했다.

> **회사 PC에서 할 일**: 이 커밋을 pull 하면 git이 그쪽에서 위 파일들을 **삭제한다.**
> `users.db`에 로그인 계정이 있으므로 **pull 전 백업 → pull → 원위치 복원** 순서로 진행할 것.

---

## 23. 남은 사항 (12절 갱신)

**해소됨**

- ~~DBAgent-Java/AIX 변경분 미커밋~~ → 22절에서 커밋·푸시 완료
- ~~SQL 작성 기능을 SQL Tune Advisor에서 활용할지 미결정~~ → `AI SQL 작성기`로 설계 확정(21절)
- ~~트레이스 연동 UI 배치 미결정~~ → 세션상세 `Tuning` 버튼이 `AI Current SQL 분석` 탭으로
  들어가는 것으로 확정(21절)

**남음**

- **다음 작업: AI DBA 메뉴 통합 구현**(21절, `매뉴통합.md` 3단계). 프롬프트 초안 2종 컨테이너 배포 포함
- `dist/databases.json` 내용 확인 — 루트와 sid/계정이 다름(13-2)
- 회사 PC에서 pull 전 `users.db` 백업(22-2)
- AIX `aidba.ollama.timeout-ms=120000`은 이 PC에만 적용됨 — 다른 환경은 수동(19절)
- sqlrestapi 개선 후보: `isConnected()` 타임아웃, (원한다면) `/api/tags`로 Ollama 실제 상태 확인
- `num_predict`/repetition penalty 미설정으로 인한 반복 루프 — 여전히 미수정
- AIX의 LIKE 키워드 폴백 미추가 — 여전히 미결
- **미착수 백로그**: RDB 대시보드 UI 개편 + 지표 5종(2026-09-06 예정이었으나 계속 밀림)
