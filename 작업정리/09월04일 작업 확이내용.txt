1. 저장소 ? 두 프로젝트 pull 완료(85070a4/be7398c). AIX의 SqlTuningController.java 로컬 작업분은 자동 병합 성공.

2. 환경설정 git 제외 ? application.properties/oracle.env/databases.json 각 8개 파일 skip-worktree.

3. Docker DB ? MariaDB(3306)/MySQL(3307)/MSSQL(1433) 신규 생성, 4종 모두 접속 검증. databases.json의 Postgres 비밀번호를 실제 값(postgres)으로 수정.

4. RDB 대시보드 ? Fleet Overview 복귀 링크 제거, PMM Annotations 삭제, Interval/Host 실동작화, 타이틀 chevron 드롭다운 구현, Detail 탭 제거 + DASHBOARD 개편, 사이드바 상단 프레임 통합.

5. 버튼/색 ? FO 솔리드 강조, 상호 이동 버튼 신설(ORA/CLE ↔ RDB, 보라 토큰), 브랜드 글씨 확대.

이번에 실제로 잡은 버그 2건

① 스코프 사고 ? 필터바 JS를 </script> 직전에 붙였는데, 이 페이지들은 전체 로직이 하나의 큰 IIFE(453~1202행) 안에 있어 dbTreeTargetPage·dbId·loadStorage가 전부 스코프 밖이었습니다. Host는 ReferenceError로 드러났지만 refreshAllPanels()는 typeof 가드 때문에 조용히 아무것도 갱신하지 않고 있었습니다 ? 타이머만 돌고 데이터는 그대로였습니다. 제가 넣은 가드가 실패를 숨긴 셈입니다.

② 에러 은폐 ? .catch(function () {...})로 에러 객체를 버려 원인 파악이 불가능했습니다. console.error + 화면 표시로 바꿨습니다.

또 pull 직후 mvn package가 반영되지 않는 함정(mtime 동일 → 리소스 복사 생략)을 겪어 이후 전부 clean 빌드로 진행했고, 메모리에도 기록했습니다.

확인 필요

- 브라우저 실렌더링 미확인 ? Chrome 확장이 계정에 연결되지 않아 정적 검증까지만 했습니다
- Oracle 화면의 RDB 버튼에 권한 게이팅 없음 ? FO는 canFleetOverview()로 숨기는데 RDB 버튼은 모두에게 보입니다. 의도하신 동작인지 확인 부탁드립니다
- stash 3건 잔존, 특히 DBAgent-Java의 stash@{1}은 출처 불명
- 전부 미커밋 상태 ? 커밋 원하시면 진행하겠습니다