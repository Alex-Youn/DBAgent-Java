# 작업정리 2026-09-05

> 대상: `DBAgent-Java`(원본) / `DBAgent-Java-AIX`(이관본) — 두 프로젝트 동일 적용
> 상태: clean 빌드 → dist 3곳 배포 → 재기동 → 브라우저 실측 검증 완료 → **커밋 완료**

시작 시점에는 09-04 작업분이 전부 미커밋이었고 톱니 아이콘 정렬 수정은 빌드 전이었다. 그 잔여분까지 함께 반영했다.

---

## 1. 계정 관리에서 RDB를 선택할 수 없던 문제

### 증상

계정 추가/수정 화면의 "접속 대상 DB" 목록에 **Oracle 8개만** 나오고 MySQL/MariaDB/PostgreSQL/MS SQL 4개가 없었다.

### 원인

`account-mgmt.html` 의 `renderDbCheckboxes()` 가 목록을 `/api/config` 가 아니라 **부모 창의 사이드바 DOM** 에서 긁어왔다.

```js
window.opener.document.querySelectorAll('#db-groups-container .instance-item[data-db-id]')
```

09-04 에 "왜 오라클 대시보드 좌측에 MySQL이 붙어 있지?" 라는 지적으로 `app.js` 에 오라클 전용 필터를 넣었는데,

```js
const oracleInstances = group.instances.filter(inst => !inst.db_type || inst.db_type === 'oracle');
```

**사이드바에서 RDB를 뺀 것이 계정 권한 화면까지 같이 지워버렸다.**

### 파급 — 단순 누락이 아니라 권한 구멍

백엔드 `RdbMonitorController` 는 `canAccessDb()` 를 8곳에서 걸고 있는데(`AuthService.java:174`), UI에서 `hidden_dbs` 에 RDB id 를 넣을 방법이 없으니 **모든 일반 계정이 RDB 4종에 무조건 접근 가능**한 상태였다.

### 함께 발견한 더 심각한 문제

계정 관리 팝업은 오라클 화면뿐 아니라 **RDB 대시보드 3종의 톱니 메뉴에서도 열린다**(`mysql-overview-dashboard.html:1276` 등). 그런데 그쪽 DOM 에는 `#db-groups-container` 도 `.top-nav .nav-item[data-target]` 도 없다(트리 id 가 `dbTree`). 그래서 RDB 화면에서 계정 관리를 열면:

| 동작 | 결과 |
|---|---|
| 메뉴/DB 체크박스 | **양쪽 다 빈 목록**으로 렌더 |
| `collectHidden()` | 빈 배열 반환 |
| 새 계정 생성 | 아무 제한 없는 전권 계정 |
| **기존 계정 수정 후 저장** | 그 계정의 **기존 메뉴·DB 제한이 통째로 초기화** |

에러도 안 나고 조용히 권한이 풀린다. `menu-visibility.html:47` 도 같은 opener 의존이라 동일 증상이었다.

### 수정

목록의 출처를 opener DOM 에서 떼어내 **`dbagent-common.js`(신규)** 로 옮겼다.

- **DB 목록** — `/api/config` 전체 인스턴스. 엔진 배지(`ORACLE`/`MySQL`/`MariaDB`/`PostgreSQL`/`MS SQL`)를 함께 표시
- **메뉴 목록** — 이 파일의 상수(7-1절에서 `DBAGENT_MENU_GROUPS` 로 그룹화됨). opener 가 오라클 화면이면 실제 nav 를 읽어 라벨 변경을 따라가고, 상수에 없는 새 메뉴는 뒤에 덧붙인다(목록에서 빠지는 쪽이 항상 더 위험하므로 병합만 하고 제거는 안 한다)
- **삭제된 인스턴스 보존** — 계정의 `hidden_dbs` 에 남아 있는데 현재 목록에 없는 id 는 `(등록되지 않음)` 행으로 함께 보여주고 체크 해제 상태를 유지한다. 예전엔 저장할 때 조용히 사라져 권한이 풀렸다
- **fail-safe** — `/api/config` 를 못 읽으면 저장 자체를 막는다(`blockedByDbLoadFailure`). 빈 목록이 "제한 없음"으로 덮어쓰는 사고를 원천 차단
- **XSS** — DB 이름을 `innerHTML` 템플릿에 끼워 넣던 것을 `textContent` 로 교체

> `admin-popup-common.js` 로 만들었다가, 오라클 화면과 RDB 대시보드도 쓰게 되면서 `dbagent-common.js` 로 개명했다.

---

## 2. 설정 창이 항목 수에 따라 커지지 않던 문제

### 원인

| 위치 | 문제 |
|---|---|
| `app.js` 팝업 호출부 | `'width=620,height=760'` **고정값** |
| `style.css:418` | `.menu-checkbox-grid` 가 `repeat(2, max-content)` **2열 고정** → 항목이 늘면 세로로만 길어짐 |
| `account-mgmt.html` | `.login-card { max-width: 1200px }` 인데 창이 620px 이라 무의미한 선언 |

### 수정 — 두 겹

1. **열 때부터 항목 수로 계산** (`dbagentAdminPopupFeatures`)
   ```
   계정 관리 : width=1066,height=700   (기존 620x760)
   메뉴 설정 : width=570,height=620    (기존 560x560)
   ```
   그리드 열 수도 항목 수에 따라 2→4열로 늘린다(`repeat(N, max-content)`, N = clamp(ceil(count/8), 2, 4)). DB 26개면 4열 + 계산 폭 1544.

2. **연 뒤 실제 콘텐츠로 재보정** (`dbagentAutoSizePopup`)
   - 가로는 `scrollWidth` 로 못 잰다(창이 좁으면 콘텐츠가 그 폭에 맞춰 줄바꿈돼 항상 `innerWidth` 언저리가 나온다). 카드를 잠깐 `width:max-content` 로 만들어 intrinsic 폭을 재고 되돌린다.
   - **`outerWidth - innerWidth` 로 창 테두리를 계산하면 안 된다.** 실측에서 팝업 inner 가 544 인데 `outerWidth` 는 부모 창 값인 1500 을 반환했다. 그래서 테두리를 계산하지 않고 **`innerWidth` 기준 2패스 보정**으로 수렴시킨다.
   - `.access-field-row` 를 flex 로 바꿔 두 목록이 각자 필요한 만큼만 차지하게 했다. `1fr` 그리드로 두면 칸 폭이 창 크기에 묶여 위 intrinsic 측정이 무력화된다.

> ⚠️ 이 절의 수치와 구조는 같은 날 **7-1절에서 다시 바뀌었다**(전체 선택/그룹 머리글 추가, 자동 크기 조정이 창을 줄이지 않도록 변경). 최종 동작은 7-1절을 볼 것.

---

## 3. 오라클 ↔ RDB 상호 이동 버튼 권한 게이팅 (사용자 요청)

**규칙: 오라클과 RDB 양쪽 모두에 접근 권한이 있는 계정에만 보인다.**

09-04 작업정리 7절의 "Oracle 화면의 RDB 버튼에는 권한 게이팅이 없다 — 의도 확인 필요" 항목에 대한 결론이다.

| 화면 | 버튼 | 이전 | 이후 |
|---|---|---|---|
| Oracle `index.html` | `⛁ RDB` | 로그인한 **모두**에게 노출 | 양쪽 권한 있을 때만 |
| RDB 대시보드 3종 | `⛁ ORA/CLE` | 게이팅 없는 단순 `<a>` | 양쪽 권한 있을 때만 |

판정은 `dbagentCrossNavAccess()` 한 곳에 모았다. 두 버튼 모두 마크업에서 `display:none` 으로 시작해 **판정에 성공했을 때만 드러난다**(fail-closed). 미로그인 시에도 감춘다(FO 버튼과 같은 방식).

**부수 수정** — 기존 RDB 버튼은 권한과 무관하게 **무조건 첫 RDB 인스턴스**로 보냈다. 그 인스턴스가 차단된 계정은 눌러도 "해당 DB에 대한 접근 권한이 없습니다" 만 봤다. 이제 **접근 가능한 첫 인스턴스**로 보낸다.

RDB 대시보드 쪽 판정은 좌측 트리가 이미 받아 둔 `cfg` 를 재사용하므로 추가 요청이 없다.

---

## 4. Interval 셀렉트의 `auto (5s)` / `5s` 중복 제거

`auto (5s)` 는 상수 `AUTO_MS = 5000` 을 쓰는데 바로 아래 `5s` 옵션과 값이 같아 고를 이유가 없었다. `auto` 옵션을 없애고 `DEFAULT_MS` 로 정리했다.

```
이전: auto (5s) | off | 5s | 10s | 30s | 1m | 5m | 직접 입력
이후:            off | 5s | 10s | 30s | 1m | 5m | 직접 입력
```

**마이그레이션 주의** — `localStorage` 에 `'auto'` 가 저장된 브라우저를 그대로 두면, 저장값이 옵션 목록에 없어 무시되고 **첫 옵션인 `off` 가 선택돼 자동 새로고침이 꺼진다.** 그래서 복원 시 `'auto'` → `'5000'` 으로 옮기고, 저장값이 없으면 명시적으로 `DEFAULT_MS` 를 선택한다.

---

## 5. MariaDB Buffer Pool Hit Rate 가 0% 로 보이던 문제

`Innodb_buffer_pool_reads` 는 논리 읽기 요청과 무관한 배경/기동 시 페이지 읽기(redo/undo, doublewrite)까지 센다. 그래서 갓 기동한 인스턴스에서는 `reads > read_requests` 가 되어 `1 - reads/read_requests` 가 **음수**가 됐고, 기존 코드는 그걸 **0 으로 클램프**했다.

문제는 "적중률 0%" 가 **"캐시가 전혀 안 맞는다"** 로 읽힌다는 것이다. 실제로는 아직 의미 있는 읽기를 처리한 적이 없을 뿐이다.

**수정** — 그 구간에서는 값을 내지 않고(`null`) 사유를 함께 준다.

```json
docker_mariadb : bufferPoolHitRatePct=null
                 note="기동 직후라 버퍼 풀 카운터가 아직 유효하지 않습니다 (reads=148 > read_requests=19)."
docker_mysql   : bufferPoolHitRatePct=93.82  note=null
```

화면은 `-` 로 표시하고 툴팁에 사유를 띄우며 게이지는 0%로 둔다. MS SQL / PostgreSQL 화면도 같은 null 처리를 넣어 `null%` 가 찍히지 않게 했다.

---

## 6. 검증

Chrome 확장은 여전히 계정에 연결되지 않아, 09-04 와 같이 로컬 Chrome 을 `--remote-debugging-port=9222` 로 띄우고 CDP 를 직접 구동했다.

### 6-1. 헤드리스 단위 검증

| 항목 | 결과 |
|---|---|
| opener 없이(=RDB 대시보드) 메뉴 목록 | **8개** (이전엔 0개) |
| opener nav 병합 | dashboard 제외, 라벨 덮어쓰기, 새 메뉴 추가 OK |
| 그리드 열 수 | 8개→2열, 26개→**4열** |
| 체크 상태 | 숨김 항목만 해제 |
| DB 이름 XSS | 마크업이 텍스트로 처리(img 생성 0) |
| `/api/config` 매핑 | Oracle/MySQL/MariaDB/PostgreSQL/MS SQL 배지 정상 |
| 게이팅 8종 시나리오 | 전부 의도대로 |

### 6-2. 로그인 후 실화면 검증

테스트 계정 2개를 **팝업 UI 로 직접 생성**해 확인하고 검증 후 삭제했다.

```
zz_test_nordb  hidden_dbs(4) = docker_mysql, docker_mariadb, docker_postgres, docker_mssql
zz_test_noora  hidden_dbs(8) = patent_integ_1·2, patent_search_1·2, patent_portal_1·2, TEST01, TEST02
```

| 계정 | Oracle 화면 `RDB` | MySQL 대시보드 `ORA/CLE` |
|---|---|---|
| `zz_test_nordb` (RDB 없음) | **숨김** | **숨김** |
| `zz_test_noora` (Oracle 없음) | **숨김** | **숨김** |
| `admin` (대조군) | 노출(flex) | 노출(flex) |

그 외 실측:

- 계정 관리 DB 목록 **12개** 전부 + 엔진 배지
- 헤더 아이콘 6개 세로 중심 **전부 38.8px 동일** (09-04 톱니 정렬 수정 확인)
- Interval 옵션 `off | 5s | 10s | 30s | 1m | 5m | 직접 입력`, 옛 `'auto'` 저장값 → `5000` 마이그레이션 확인
- MariaDB Hit Rate `-` + 툴팁 사유 + 게이지 0%
- 팝업 크기: 요청 1066×700 → 메인 뷰 inner 958×741 → 계정추가 뷰 **1102×929**, **가로·세로 스크롤 불필요**

### 6-3. 검증 중 겪은 함정 (메모리에 기록)

- **`ConvertTo-Json` 이 반환되지 않는다.** Git Bash 에서 `powershell -File` + 스트림 리다이렉트 조합이면 입력이 4글자여도 멈춘다. 보낼 JSON 은 문자열로 직접 조립해야 한다(받는 쪽 `ConvertFrom-Json` 은 정상).
- **같은 연결에서 두 번째 `SendAsync` 가 블로킹된다.** `Runtime.evaluate` 는 `Runtime.enable` 없이도 되므로 명령을 하나만 보내는 구조로 짰다.
- **팝업 크기는 프로필 상태에 오염된다.** 어떤 임시 프로필에서는 `window.open(...width=1120...)` 도 `resizeTo()` 도 전부 무시되고 inner 가 계속 544 로 나왔다. **새 `--user-data-dir` 로 띄우니 정상**이었다(1066 요청 → inner 958). 창 크기 문제는 코드를 의심하기 전에 프로필을 새로 만들어 재현할 것.
- 한 번의 `Runtime.evaluate` 에서 `window.open` 은 **첫 호출만** 허용된다(팝업 차단).
- **서버를 재기동하면 로그인 세션이 죽는다.** sessionStorage 에 토큰이 남아 화면은 로그인 상태로 보이는데 API 가 401 이다. 재기동이 잦은 작업이면 배포를 다 끝낸 뒤 마지막에 로그인할 것.

---

## 7. 정리 작업

**stash 3건 제거** — 09-04 부터 남아 있던 것으로, 내용은 전부 설정/런타임 산출물이었다.

| 저장소 | stash | 내용 |
|---|---|---|
| DBAgent-Java | `pre-pull-2026-09-04` | databases.json, 로그, pid, users.db, target 산출물 |
| DBAgent-Java | `sync: local runtime artifacts before pull` | application.properties, oracle.env, start/stop.bat, 로그 |
| DBAgent-Java-AIX | `pre-pull-2026-09-04` | 로그, application.properties, `SqlTuningController.java` |

AIX stash 의 `SqlTuningController.java` 는 현재 워킹트리가 **상위 집합**임을 확인했다(워킹트리에는 원격에서 온 null 체크가 추가로 들어가 있고 stash 판에는 없다). 안전을 위해 **세 건 모두 `backup_stash_2026-09-05/*.patch` 로 내보낸 뒤** drop 했다.

---

## 7-1. 관리자 팝업 2차 개선 (전체 선택 / 메뉴 그룹 / 창 크기)

사용자 지적 3건을 반영했다.

### (1) 전체 선택 체크박스

새 계정 추가 시 메뉴·DB 체크박스가 **전부 체크된 상태**로 시작해, 일부만 남기려면 하나씩 눌러야 했다. 두 목록 맨 위에 `전체 선택`을 넣었다.

- 클릭하면 그 목록 전체를 켜고 끈다
- 일부만 체크된 상태에서는 `indeterminate`(중간 표시)
- 이 체크박스에는 `data-menu-target` / `data-db-target` 속성을 **붙이지 않는다.** 저장할 때 `collectHidden()` 이 `input[attr]` 로만 수집하므로 목록 값에 섞이지 않는다 — `input[type="checkbox"]` 로 긁던 기존 선택자를 그대로 뒀으면 `null` 이 `hidden_menus` 에 들어갔을 것이다

### (2) Oracle 메뉴 / RDB 메뉴 구분

메뉴 목록이 오라클 화면 메뉴만 담고 있어 RDB 대시보드 메뉴는 설정할 수 없었다. `DBAGENT_MENU_GROUPS` 로 그룹을 나눴다.

| 그룹 | 항목 |
|---|---|
| Oracle 메뉴 | Current Session, 성능 이력 조회, Lock Holder/Waiter Tree, 테이블 스페이스 조회, Table Parent/Child 관계, SQL 실행, AI DBA, SQL 정합성/튜닝 (8개) |
| RDB 메뉴 | DASHBOARD (1개, id `rdb_dashboard`) |

- 오라클 화면의 `DASHBOARD` 는 예전처럼 숨김 대상이 아니다(항상 표시). RDB 쪽은 id 가 `rdb_dashboard` 로 따로라 서로 겹치지 않는다
- **RDB 대시보드가 이 설정을 실제로 반영하도록** `dbagentApplyRdbMenuVisibility()` 를 붙였다(오라클 화면의 `applyMenuVisibility()` 대응). 브라우저별 설정(`localStorage`)과 계정 권한(`sessionStorage`) 합집합으로 판단한다
- 지금은 RDB 탭이 DASHBOARD 하나뿐이고 본문은 탭과 무관하게 항상 보이므로, 숨기면 **탭 버튼만** 사라진다. 탭이 늘면 `DBAGENT_MENU_GROUPS` 의 rdb 그룹에 추가하면 된다
- 계정에 남아 있는데 어느 그룹에도 없는 메뉴 id 는 `등록되지 않은 메뉴` 그룹으로 따로 보여준다(저장 시 조용히 사라지지 않게)

### (3) 창 크기 — 글씨가 팝업 밖으로 삐져나오던 문제

`메뉴 표시 설정` 팝업에서 문서 폭 438px 가 창 안쪽 404px 를 넘어 글씨가 잘렸다. 원인이 두 개였다.

**원인 A — `resizeTo` 는 창이 만들어지는 동안 호출하면 무시된다.** 초기 렌더에서 부른 자동 크기 조정이 통째로 먹지 않았다. `load` 이후로 미뤄 호출하도록 고쳤다.

**원인 B — 자동 크기 조정이 창을 줄이면 다시 못 키운다.** 실측 결과 브라우저는 **창 생성 직후 한 번만** `resizeTo` 를 받아주고 그 뒤 호출은 조용히 무시한다(opener 에서 불러도, 팝업 자신이 불러도 동일). 계정 관리 팝업은 처음에 작은 메인 뷰가 보이므로, 자동 조정이 창을 1066 → 560 으로 **줄여버린 뒤** 계정 추가 뷰로 바꿀 때 다시 키우지 못해 콘텐츠가 잘렸다.

→ 자동 크기 조정을 **키우기만 하고 줄이지는 않도록** 바꿨다. `window.open` 이 항목 수로 계산해 준 크기가 하한이 된다.

추가로 `.checkbox-list` 에 `max-width:100% / overflow-x:auto` 를 줘서, 창을 못 키우는 환경에서도 글씨가 팝업 밖으로 나가지 않고 목록 안에서 스크롤되게 했다(라벨이 `nowrap` 이라 줄바꿈으로는 줄어들지 않는다).

### (4) 그룹 머리글 좌측 정렬 + 액센트

`.login-card` 가 `text-align:center` 라 메뉴 표시 설정 팝업의 그룹 머리글이 가운데로 밀렸다(계정 관리 쪽은 감싸는 div 에 인라인 `text-align:left` 가 있어 왼쪽이었다). `.checkbox-list` 에 `text-align:left` 를 주고, 머리글에 **왼쪽 3px 색 막대 + 강조색(`--primary`) + `font-weight:700`** 으로 액센트를 넣었다.

### 실측 결과

| 항목 | 결과 |
|---|---|
| 메뉴 표시 설정 창 | inner 554×553, **가로·세로 삐져나감 없음** (이전 404 폭에 438 콘텐츠) |
| 계정 관리(계정 추가 뷰) | 요청 1066×772 → inner 1050×705, 문서 1050×705 — **정확히 맞음** |
| 그룹 머리글 | `text-align:left`, `color rgb(57,135,229)`, `border-left 3px`, `weight 700` |
| 전체 선택 | 해제 → 0/9, 재선택 → 9/9, 하나만 해제 → `indeterminate=true` |
| 저장값 오염 | DB 전체 해제 시 미체크 12개, `null` 포함 **없음** |
| RDB 탭 숨김 | 기본 노출 → `localStorage` 숨김 시 숨김 → 계정 권한 숨김 시 숨김 → 되돌리면 노출 |

---

## 7-2. SQL 정합성/튜닝 sLLM 연동 (두 프로젝트 모두 실제 연동으로 전환)

> **회사에서 이 절만 봐도 되도록 자체 완결로 적는다.**

### 배경

`SQL 정합성/튜닝` 메뉴의 모델 기반 분석은 **두 프로젝트 모두 스텁**이었다. 버튼을 누르면 항상 고정 안내문만 반환했다.

```
SQL 튜닝 sLLM 서버가 아직 연동되어 있지 않습니다. 추후 FastAPI 서버 연결 예정입니다.
지금은 "1차 성능점검" 버튼으로 실행계획/실측 통계만 직접 확인해주세요.
```

원래는 실제 호출을 시도했는데, 서버가 없으면 **10초 connect timeout 뒤 커넥션 거부 에러가 화면에 그대로 노출**돼서 2026-08-29에 통째로 스텁 처리했던 것이다.

그 사이 AIX 이관본 쪽에 sLLM 연동 코드가 작성돼 미커밋으로 남아 있었다(2026-09-02). 로컬 WSL 에서 **FastAPI + PEFT(QLoRA Qwen2.5-Coder-7B)** 로 테스트를 마친 작업분이고, **폐쇄망에서는 GPU 서버를 가리켜 사용할 예정**이다. 이걸 커밋하면서 원본에도 역포팅해 두 프로젝트를 다시 맞췄다.

### 구현

| 항목 | DBAgent-Java (Java 17) | DBAgent-Java-AIX (Java 8) |
|---|---|---|
| HTTP 클라이언트 | `java.net.http.HttpClient` | `RestTemplate` + `SimpleClientHttpRequestFactory` |
| 연결 실패 예외 | `java.net.ConnectException` / `HttpTimeoutException` | `ResourceAccessException` |
| 그 외 | 동일 | 동일 |

`java.net.http.HttpClient` 는 Java 11+ 전용이라 AIX 이관본에서는 쓸 수 없어 `RestTemplate` 으로 다시 썼다(`OllamaChatService` 의 `RestTemplate` 재작성과 같은 이유). 계약(`POST <url>/generate` → `{"response": "..."}`)은 동일하다.

동작하게 된 엔드포인트는 세 개다.

- `POST /api/sqltuning/analyze` — 사용자가 붙여넣은 쿼리/실행계획을 그대로 분석
- `POST /api/sqltuning/analyze_from_query` — `EXPLAIN PLAN` 으로 실행계획을 뽑아 이어서 분석 (관리자 전용)
- `POST /api/sqltuning/analyze_from_query_actual` — 쿼리를 실제 실행해 `DBMS_XPLAN.DISPLAY_CURSOR(ALLSTATS LAST)` 실측치로 분석 (관리자 전용)

`quick_check` / `bind_capture` 는 원래부터 모델 없이 Oracle 조회만으로 동작한다 — **sLLM 이 없어도 그대로 쓸 수 있다.**

### 설정 (`application.properties`)

```properties
sqltuning.api.url=http://localhost:8010
sqltuning.api.timeout-ms=180000
```

| 속성 | 기본값 | 설명 |
|---|---|---|
| `sqltuning.api.url` | `http://localhost:8010` | FastAPI sLLM 서버 주소. **localhost 가 아니어도 된다** — 평문 HTTP/JSON 만 쓰므로 같은 머신이든 LAN 의 GPU 서버든 동일하게 동작한다(`aidba.ollama.url` 과 같은 패턴). 폐쇄망에서는 GPU 서버 주소로 바꿀 것 |
| `sqltuning.api.timeout-ms` | `180000` (3분) | 응답 대기 시간. 예전엔 180초 하드코딩이었는데 **GPU 서버 성능과 프롬프트 길이에 따라 크게 달라져 설정으로 뺐다.** connect timeout 은 별도로 10초 고정 |

> **⚠️ 기본값을 지운 채 쓰지 말 것.** 이 속성들은 `application.properties` 에만 있는데 그 파일은 환경마다 값이 달라 **커밋 대상에서 빠져 있다**(2026-09-04 결정, skip-worktree). `@Value` 에 기본값이 없으면 속성이 없는 환경에서 플레이스홀더 해석에 실패해 **SQL 튜닝 기능만이 아니라 앱 전체가 기동하지 못한다.** 그래서 두 프로젝트 모두 `${sqltuning.api.url:http://localhost:8010}` 형태로 기본값을 박아 뒀다.

### 화면에서 보이는 모습

`app.js` 는 `success:false` 면 `message` 를 **그대로 빨간 글씨로 출력**한다(`app.js` 의 `sqlTuningResult.innerHTML`). 그래서 예외 문자열을 그대로 흘리면 안 된다.

| 상황 | 화면 |
|---|---|
| sLLM 도달 가능 | 분석 결과 + `[+] 조회된 실행계획 원문 보기` 접기 패널 |
| **sLLM 도달 불가** | `SQL 튜닝 sLLM 서버(http://localhost:8010)에 연결할 수 없습니다. 서버가 켜져 있는지, 이 호스트에서 도달 가능한 주소인지 확인해주세요. 지금은 "1차 성능점검" 버튼으로 실행계획/실측 통계만 확인할 수 있습니다.` |
| 등록되지 않은 `db_id` | `등록되지 않은 DB입니다.` |
| 빈 입력 | `쿼리/실행계획을 입력해주세요.` |
| 그 외 (HTTP 4xx/5xx, 응답 파싱 실패 등) | `<작업명> 오류: <원인>` |

연결 실패 시 **원인은 서버 로그(WARN)에만** 남는다.

```
WARN c.dbagent.sqltuning.SqlTuningController : SQL 튜닝 모델 서버 연결 실패
  (url=http://localhost:8010): java.net.ConnectException
```

이 처리(`modelErrorMessage`)가 08-29에 스텁으로 막았던 문제를 대신 해결한다. 즉 **sLLM 이 없는 환경에서도 기존과 같은 수준의 안내가 유지되므로, 스텁으로 되돌릴 필요가 없다.**

### 함께 고친 것 — `analyze_from_query*` 의 `db_id` 검증 누락

두 엔드포인트가 `configService.resolve(dbId, account)` 결과를 **null 체크 없이** `explain()` 에 넘기고 있었다. 등록되지 않은 `db_id` 가 오면 엉뚱한 DB 로 흘러가거나 NPE 가 난다. `quick_check` / `bind_capture` 에는 이미 있던 체크를 이 두 곳에도 넣었다. `dbagent-java-overview` 메모리에 기록된 "db_id 누락 → 엉뚱한 DB 접속" 계열의 문제다.

### 확인한 내용

sLLM 미기동 상태에서 두 프로젝트 각각 실제 호출로 확인했다(원본 8006, AIX 빌드는 8007 로 임시 기동).

```
POST /api/sqltuning/analyze {"prompt":"select 1 from dual"}
-> {"success":false,"message":"SQL 튜닝 sLLM 서버(http://localhost:8010)에 연결할 수 없습니다. ..."}
   로그: WARN ... java.net.ConnectException (원본) / ResourceAccessException: Connection refused (AIX)

POST /api/sqltuning/analyze {"prompt":"  "}
-> {"success":false,"message":"쿼리/실행계획을 입력해주세요."}
```

**sLLM 서버가 실제로 떠 있는 상태의 분석 결과는 확인하지 못했다** — 이 PC 에서 WSL sLLM 을 기동하지 않았다. 회사에서 GPU 서버를 붙인 뒤 `analyze` 정상 응답을 한 번 확인할 것.

### 폐쇄망 배포 시 체크리스트

1. `application.properties` 의 `sqltuning.api.url` 을 **GPU 서버 주소**로 설정
2. AIX 호스트에서 도달 가능한지 먼저 확인 — `curl <url>/health` 또는 `curl <url>/generate`
3. GPU 서버 응답이 3분을 넘으면 `sqltuning.api.timeout-ms` 를 늘릴 것
4. 도달 불가 상태로 배포해도 **앱은 정상 기동**하고 `1차 성능점검` 은 그대로 동작한다

---

## 8. 변경 파일

두 저장소 동일. 신규 1개 + 수정 9개.

| 파일 | 내용 |
|---|---|
| `static/dbagent-common.js` | **신규** — 메뉴 상수, `/api/config` 파싱·캐시, 권한 판정, 그리드 렌더, 팝업 크기 계산/보정 |
| `static/app.js` | RDB 버튼 게이팅 + 접근 가능한 인스턴스로 이동, 팝업 크기 계산 적용 |
| `static/index.html` | 스크립트 로드, RDB 버튼 초기 숨김, `app.js?v=87` |
| `static/account-mgmt.html` | 목록 출처 교체, 저장 차단 가드, 창 자동 크기, `style.css?v=26` |
| `static/menu-visibility.html` | 메뉴 목록 출처 교체, 창 자동 크기 |
| `static/mysql`/`postgres`/`mssql-overview-dashboard.html` | 스크립트 로드, ORA/CLE 게이팅, Interval 중복 제거, Hit Rate null 처리 |
| `static/style.css` | `.gear-menu-wrap` flex 정렬(09-04 잔여분), `.menu-checkbox-grid` → `.checkbox-list` 구조로 교체(전체 선택/그룹 머리글) |
| `rdb/MySqlMonitorService.java` | Buffer Pool Hit Rate 를 클램프 대신 null + 사유로 |
| `sqltuning/SqlTuningService.java` | sLLM 연동 — AIX 는 **신규**(RestTemplate), 원본은 `@Value` 기본값 + `timeout-ms` 설정화 + `apiUrl()` 추가 |
| `sqltuning/SqlTuningController.java` | 스텁 → 실제 호출. `analyzeWithPlan` / `modelErrorMessage` 추가, `analyze_from_query*` 에 `db_id` null 체크 |

**캐시버스터** — `app.js?v=87`, `dbagent-common.js?v=3`, `style.css?v=27`. 세 번호 모두 두 저장소 히스토리 전체에서 한 번도 쓰인 적 없음을 `git log -S` 로 확인했다(번호 재사용으로 브라우저 캐시가 옛 파일을 물던 08-29 사고 방지).

**빌드/배포** — 두 저장소 `mvn clean package`(stale 0건) → dist 3곳 jar 교체(`.bak-*` 백업) → 8006 재기동. 서빙 콘텐츠 == 소스 9개 파일 전부 일치 확인.

---

## 9. 남은 사항

- `dist-aix` 는 폐쇄망 AIX 전용이라 로컬 기동 검증은 생략했다(jar 만 교체).
- `MsSqlMonitorService` / `PostgresMonitorService` 의 적중률 산식 자체는 손대지 않았다(음수가 나올 구조가 아니다). 화면 쪽 null 처리만 넣었다.
- 검증용 임시 Chrome 프로필은 스크래치패드에 남아 있다(`--user-data-dir`).
- **sLLM 서버가 실제로 떠 있는 상태의 분석 결과는 미확인** — 회사 GPU 서버 연결 후 한 번 확인할 것(7-2절 체크리스트).

---

## 10. 회사에서 pull 할 때 주의

**`git pull` 직후에는 반드시 `mvn clean package`.** `clean` 없이 빌드하면 `src/main/resources/static/*` 와 `target/classes/static/*` 의 mtime 이 같아 maven-resources-plugin 이 복사를 건너뛴다. 빌드 로그에 `Copying 25 resources` 가 찍히는데도 **옛 정적 파일이 그대로 jar 에 들어간다**(09-04 실제 사고).

이번 커밋에는 신규 파일 `static/dbagent-common.js` 가 있고 `index.html` 이 이걸 로드한다. 누락되면 에러 없이 조용히 깨진다 — 상호 이동 버튼이 안 보이거나, 계정 관리 팝업의 메뉴/DB 목록이 비어 보인다.

배포 전 검증 한 줄:

```bash
for f in src/main/resources/static/*; do b=$(basename "$f");
  diff -q "$f" "target/classes/static/$b" >/dev/null 2>&1 || echo "STALE $b"; done
```

`application.properties` / `oracle.env` / `databases.json` 은 `skip-worktree` 로 커밋 대상에서 빠져 있으므로 pull 해도 회사 값이 덮이지 않는다. 다만 **회사 PC 에는 skip-worktree 가 적용돼 있지 않을 수 있다**(이 설정은 로컬 전용) — pull 전에 `git ls-files -v | grep '^S '` 로 확인할 것.

---

## 11. RDB 대시보드 세션 화면 3종 추가 (세션 리스트 / 세션 상세 / Lock 모니터링)

> 같은 날 이어서 진행한 별도 작업. 근거 문서: `세션리스트 및 세션 정보 조회 쿼리.md`
> 두 저장소(`DBAgent-Java`, `DBAgent-Java-AIX`) 동일 적용 · 빌드 → dist 3곳 배포 → 실화면 검증 완료

### 배경

RDB 대시보드는 탭이 `DASHBOARD` 하나뿐이었다. 오라클 화면에는 있는 세션 리스트 / 세션 상세 /
Lock 모니터링이 RDB 쪽에는 없어서, 문서의 쿼리로 채워 넣었다.

### 11-1. 백엔드

`EngineMonitorService` 에 메서드 3개를 추가하고 엔진별 구현체 3곳에 구현했다.

| API | 근거 절 |
|---|---|
| `GET /api/rdb/session_list` | 3.1 / 4.1·4.2 / 5.1 |
| `GET /api/rdb/session_detail` | 3.2 / 4.3 / 5.2 |
| `GET /api/rdb/lock_waits` | 3.3 / 4.4·4.5 / 5.3 |

세 엔드포인트 모두 `canAccessDb(token, db_id)` 를 건다 — 세션 목록과 실행 중인 SQL 전문은 이 앱이
내보내는 정보 중 가장 민감한 축이다(다른 팀의 쿼리 원문이 그대로 보인다).

`session_id` 는 사용자가 고른 값이 쿼리에 들어가는 유일한 자리라, 문자열로 받아 `Long.parseLong`
으로 검증한 뒤 `PreparedStatement` 로 바인딩한다. 숫자가 아니면 DB 까지 보내지 않는다.

```
GET /api/rdb/session_detail?db_id=docker_mysql&session_id=abc'--
-> {"error":"세션 ID가 올바르지 않습니다."}
```

**응답을 백엔드에서 정규화했다.** 엔진마다 컬럼 이름도 단위도 다른데(MS SQL 만 밀리초, MySQL 은
`program` 개념이 없음), 화면에서 엔진별 렌더링을 분기하면 세 대시보드 × 두 저장소로 금방 흩어진다.
그래서 서버가 공통 키로 맞춰 내려주고, **화면 코드에는 `db_type` 분기가 한 줄도 없다.**

- 세션 리스트 — `session_id / user / host / db / program / status / duration_seconds / wait_event / query_preview`
- 세션 상세 — `found / session_id / fields[{label,value}] / sql_text` (항목이 엔진마다 다르므로 서버가 순서대로 주고 화면은 그대로 그린다)
- Lock — `waiter_* / wait_duration_sec / wait_type / blocker_*`

### 11-2. 문서의 쿼리 오류 2건 (실행하면 바로 깨지는 것)

문서를 그대로 옮기기 전에 로컬 컨테이너에서 전부 실행해봤고, MySQL·MariaDB Lock 쿼리 두 개가
**둘 다 `ERROR 1054` 로 실패**했다. 문서(4.4 / 4.5)도 같이 고쳐뒀다.

| 절 | 원문 | 실제 | 확인 환경 |
|---|---|---|---|
| 4.4 MySQL | `data_lock_waits.requested_lock_mode` / `.blocking_lock_mode` 를 읽음 | **그 뷰에 락 모드 컬럼이 없다.** `data_locks` 를 요청/블로킹 `engine_lock_id` 로 각각 조인해야 함 | MySQL 8.4.11 |
| 4.5 MariaDB | `innodb_lock_waits.requesting_lock_id` | 실제 이름은 **`requested_lock_id`** | MariaDB 11.8.9 |

4.5 는 이 뷰가 트랜잭션 id 도 함께 주므로, 원문처럼 `innodb_locks` 를 거쳐 트랜잭션을 찾아갈 필요
없이 `innodb_trx` 로 바로 조인해 조인 수를 줄였다. 락 모드/대상 테이블만 `innodb_locks` 에서
`LEFT JOIN` 으로 붙인다 — 락 행이 그 사이 사라져도 대기 관계 자체는 사라지지 않게.

**MS SQL 5.3 도 보완했다(에러는 아니지만 위험).** 원문은 대기 세션의 요청(`r_w`)과 블로커
세션(`s_b`)을 `INNER JOIN` 하는데, **블로커가 `BEGIN TRAN` 후 아무 요청도 안 돌리는 경우가 실제
블로킹의 상당수다.** `INNER JOIN` 이면 그런 행이 통째로 사라져 "막혀 있는데 Lock 화면은 비어 있는"
최악의 오해를 만든다. 전부 `LEFT JOIN` 으로 바꾸고 블로커 상태는 `COALESCE(r_b.status, s_b.status)`
로 채웠다.

문서 7절의 나머지(MariaDB 에 `performance_schema.processlist` 가 없고, `INNODB_LOCKS` 는 있다)는
이번에도 그대로 재확인됐다.

### 11-3. 구현하면서 문서와 달리 한 것

| 항목 | 이유 |
|---|---|
| MySQL/MariaDB 판정을 `db_type` 이 아니라 **JDBC 메타데이터 제품 버전 문자열**로 | `db_type` 을 `mysql` 로 잘못 등록한 MariaDB 에 MySQL 전용 쿼리를 날리면 `ERROR 1146` 으로 화면이 통째로 죽는다. 설정 실수치고 대가가 너무 크다. 메타데이터는 접속 시 이미 받아 둔 값이라 추가 쿼리가 없다 |
| 세션 리스트에서 `command = 'Daemon'` 도 제외 | 안 빼면 `event_scheduler` 한 줄이 목록 맨 위에 상주해 실제 세션을 밀어낸다 |
| PostgreSQL 세션 리스트에 `datname` 필터 없음 | 인스턴스 전체의 활성 세션을 보는 화면이다. 다른 DB 의 무거운 쿼리가 목록에서 사라지면 못 찾는다. 대신 `db` 컬럼으로 구분 |
| PostgreSQL `client_addr` 가 NULL 이면 `'local'` | 빈 칸은 조회 실패처럼 보인다 |
| 세션 상세는 유휴/자기 세션 필터 없음 | 목록을 그린 뒤 클릭하기까지 사이에 유휴로 바뀌었다고 "없음" 으로 보이면 혼란스럽다. 진짜 끝난 경우만 `found:false` → `"이 세션은 이미 종료되었습니다"` |
| MS SQL 상세는 `dm_exec_sessions` 기준 `LEFT JOIN` | 요청이 끝난 세션도 접속 정보는 보여주려고 |
| MS SQL 리스트 `status` 에 `command` 를 괄호로 덧붙임 | `suspended` 한 단어보다 `suspended (UPDATE)` 가 훨씬 읽힌다 |

### 11-4. 프론트엔드 — 신규 `static/rdb-session-views.js`

화면이 세 대시보드에 똑같이 들어간다. HTML 세 곳에 표 렌더링을 복사하면 두 저장소까지 합쳐
**여섯 벌**이 되고 그 중 하나만 고치는 사고가 난다. 그래서 HTML 에는 탭 버튼과 빈 컨테이너만 두고
(세 파일 모두 같은 id), **표·모달·CSS·탭 전환을 전부 이 파일이 만든다.**

`dbagent-common.js` 에 넣지 않은 이유는 그 파일이 `index.html` 과 관리자 팝업에도 로드되기 때문 —
그쪽 화면에는 필요 없는 코드다.

- **SQL 원문은 반드시 `textContent` 로만 넣는다.** 여기 흐르는 문자열은 다른 사용자가 실행 중인
  쿼리라 이 앱이 통제할 수 없는 값이다. `innerHTML` 로 넣으면 그대로 XSS 다
- **보이는 탭만 새로고침한다.** 세 탭을 항상 갱신하면 안 보는 화면 때문에 DB 에 매 주기 쿼리가
  두 배로 나간다(세션/락 쿼리는 둘 다 시스템 뷰 전체를 훑는다)
- 선택한 탭은 `localStorage('rdb_active_tab')` 에 기억한다
- Lock 표는 한 행에 대기/유발 두 세션이 들어 있어 눈으로 경계를 못 찾는다 → 대기 쪽을 주황색으로,
  유발 쪽 첫 칸에 세로 구분선을 넣었다
- 2026-09-04 에 `.view-tab` 을 `cursor:default` 로 바꿔뒀던 것(탭이 하나뿐이라 "누를 수 있는 것처럼
  보이지 않게")을 되돌렸다. 이제 실제로 전환되는 탭이다

### 11-5. 메뉴 표시 설정 연동

`DBAGENT_MENU_GROUPS` 의 rdb 그룹에 `rdb_sessions`(세션 리스트) / `rdb_locks`(Lock 모니터링)를
추가했다. `dbagentApplyRdbMenuVisibility()` 도 탭 3개를 다루도록 고쳤다.

**탭이 하나뿐일 땐 버튼만 감추면 끝이었지만, 이제는 "지금 보고 있는 탭이 숨겨지는" 경우가 생긴다.**
그대로 두면 탭 버튼은 사라졌는데 그 본문만 덩그러니 남는다. 그래서 숨김 적용 후
`dbagentSyncRdbActiveTab()` 으로 남아 있는 첫 탭으로 옮긴다.

실측:

| 단계 | 결과 |
|---|---|
| Lock 탭 선택 | `tabLocksBtn` active, `locksView` 표시 |
| `rdb_locks` 숨김 | 버튼 숨김 + **DASHBOARD 로 자동 이동** |
| `rdb_dashboard` 도 숨김 | 남은 **세션 리스트로 자동 이동** |
| 숨김 해제 | 세 버튼 모두 복귀 |

관리자 팝업 메뉴 목록도 `RDB 메뉴: DASHBOARD, 세션 리스트, Lock 모니터링` 3개로 확인.

### 11-6. 검증

Chrome 확장은 **여전히 계정에 연결되지 않아**(이번이 3회째) 09-04·09-05 와 같이 로컬 Chrome 을
`--remote-debugging-port=9222` + 새 `--user-data-dir` 로 띄우고 CDP 를 직접 구동했다. 로그인 UI 를
거치지 않고 `/api/login` 으로 받은 토큰을 `sessionStorage` 에 직접 심어 검증했다 — 09-05 에 겪은
"사용자에게 CDP 창을 지목하기 어렵다" 문제를 아예 우회한다.

4개 엔진에 각각 holder/waiter 트랜잭션으로 **실제 블로킹을 만들어** API 와 화면을 확인했다.

| DBMS | 버전 | 세션 리스트 | 세션 상세 | Lock 체인 |
|---|---|---|---|---|
| MySQL | 8.4.11 | ✅ | ✅ | ✅ `X,REC_NOT_GAP (testdb.lock_test_tbl)` |
| MariaDB | 11.8.9 | ✅ | ✅ | ✅ `` X (`testdb`.`lock_test_tbl`) `` |
| PostgreSQL | 16.15 | ✅ | ✅ | ✅ `Lock / transactionid` |
| MS SQL Server | 2022 | ✅ | ✅ | ✅ `LCK_M_X` |

그 외 실측:

- 브라우저 JS 에러 **0건** (모든 단계에서 `window.__errs` 빈 배열)
- 세션 상세 모달: 필드 7~10개 + SQL 전문 + 복사 버튼. 이미 끝난 세션을 클릭하면
  `"이 세션은 이미 종료되었습니다. 목록을 새로고침해주세요."`
- 토큰 없이 호출 → `{"error":"해당 DB에 대한 접근 권한이 없습니다."}`
- AIX 이관본(Java 8 / Spring Boot 2.7)을 8007 로 임시 기동해 **같은 4엔진 × 3 API 전부 동일 응답 확인**
  (`maven.compiler.release=8` 이라 Java 8 비호환 문법은 컴파일에서 막힌다)

락 테스트용 `lock_test_tbl(id, val)` 테이블을 네 엔진의 `testdb` 에 만들어 뒀다(재현용으로 유지).
검증 후 테스트 세션은 전부 KILL 로 정리했다.

### 11-7. 변경 파일

두 저장소 동일. 신규 1개 + 수정 8개.

| 파일 | 내용 |
|---|---|
| `static/rdb-session-views.js` | **신규** — 탭 전환, 세션/Lock 표, 상세 모달, CSS 주입 |
| `rdb/EngineMonitorService.java` | `getSessionList` / `getSessionDetail` / `getLockWaits` 추가 + 공통 키 규약 문서화 |
| `rdb/MySqlMonitorService.java` | MySQL 8 / MariaDB 분기(메타데이터 판정), 정정된 Lock 쿼리 2종 |
| `rdb/PostgresMonitorService.java` | 문서 3절 구현 |
| `rdb/MsSqlMonitorService.java` | 문서 5절 구현 + `LEFT JOIN` 보완 |
| `rdb/RdbMonitorController.java` | 엔드포인트 3개 + `session_id` 검증 (AIX 는 `Maps.of`) |
| `static/dbagent-common.js` | rdb 메뉴 그룹 3개로 확장, `dbagentApplyRdbMenuVisibility` 가 탭 3개 처리 + 활성 탭 재동기화 |
| `static/mysql`/`postgres`/`mssql-overview-dashboard.html` | 스크립트 로드, 탭 버튼 2개, 뷰 컨테이너 2개, `refreshAllPanels` 훅 |
| `static/index.html`, `account-mgmt.html`, `menu-visibility.html` | 캐시버스터만 (`dbagent-common.js?v=4`) |

**캐시버스터** — `dbagent-common.js?v=4`, `rdb-session-views.js?v=1`. 두 번호 모두 두 저장소
히스토리 전체에서 한 번도 쓰인 적 없음을 `git log -S` 로 확인했다. `app.js` / `style.css` 는 안 고쳤다.

**빌드/배포** — 두 저장소 `mvn clean package`(stale 0건) → dist 3곳 jar 교체(`.bak-*` 백업) →
원본 8006 재기동. 서빙 콘텐츠 == 소스 일치 확인.

### 11-8. 남은 사항

- 문서 **6절의 추가 지표 5종**(커넥션 풀 고갈율 / 장기 트랜잭션 / 리플리케이션 지연 / 버퍼 캐시
  히트율 / Temp·로그 용량)은 이번 범위에 넣지 않았다. 버퍼 캐시 히트율만 기존 DASHBOARD 탭 KPI
  카드에 이미 있고 나머지 넷은 미구현이다.
- 세션 **Kill 기능은 넣지 않았다**(문서에 없고 요청도 없었다). 오라클 화면에는 있으므로, 필요하면
  같은 정규화 구조 위에 추가하면 된다.
- `rdb-dashboard.html`(등록되지 않은 db_type 용 폴백 페이지)에는 이 탭들을 붙이지 않았다.
- `dist-aix` 는 폐쇄망 AIX 전용이라 jar 만 교체했다(8007 임시 기동으로 API 는 검증함).

---

## 12. 세션 Kill(관리자 전용) + Lock 화면을 Holder/Waiter 트리로

> 11절에 이어 같은 날 진행. 사용자 요청 2건: **선택 세션 Kill**, **Lock을 Holder/Waiter 트리 형식으로**.
> 추가 지시: **"세션 kill 은 관리자 권한이 있는 유저만 가능해야해"**

### 12-1. 세션 Kill — 권한을 조회와 분리

`POST /api/rdb/kill_session?db_id=` (body `{"sessions":["12","34"],"token":"..."}`)

**조회 API 는 `canAccessDb`, Kill 은 `isAdmin` 이다.** 같은 급으로 두면 "볼 수 있으면 죽일 수도
있는" 상태가 되는데, 세션 Kill 은 남의 트랜잭션을 롤백시키는 파괴적 동작이다. 오라클 화면의
`kill_session`(`MonitorController.killSession`)도 `isAdmin` 만 본다 — 같은 기준으로 맞췄다.

화면에서도 Kill 버튼과 체크박스를 관리자에게만 그리지만 **그건 편의일 뿐 권한 판정이 아니다.**
`sessionStorage` 의 role 은 브라우저에서 고칠 수 있으므로 **실제 차단은 서버에서만** 한다.

| DBMS | 명령 | 함정 |
|---|---|---|
| MySQL / MariaDB | `KILL <id>` | `KILL QUERY` 가 아니라 커넥션을 끊는다(오라클 kill 과 같은 의미). 없는 세션이면 `ER_NO_SUCH_THREAD(1094)` → "이미 종료된 세션입니다."로 바꿔 보여준다 |
| PostgreSQL | `pg_terminate_backend(<pid>)` | 대상이 없으면 **예외가 아니라 false** 를 돌려준다. 그걸 성공으로 세면 "성공 1건" 인데 세션은 그대로인 것처럼 보인다 |
| MS SQL | `KILL <spid>` | `ALTER ANY CONNECTION` 권한 필요. 시스템 세션(SPID ≤ 50)과 자기 자신은 불가 |

**주입 방어가 조회보다 얇다는 점에 주의.** 조회 API 는 `PreparedStatement` 로 바인딩하지만,
**MySQL·MS SQL 의 `KILL` 은 바인드 파라미터를 받지 못하는 명령**이라 문자열로 조립할 수밖에 없다
(PostgreSQL 만 함수라 바인딩된다). 그래서 `Long.parseLong` 검증이 **유일한 방어선**이고, 그래서
`EngineMonitorService.killSession()` 의 시그니처를 String 이 아니라 **long** 으로 뒀다 — 검증되지
않은 값이 타입 수준에서 못 들어오게.

여러 건을 한 번에 보내고, 한 건이 실패해도 나머지는 계속 진행하며 건별 결과를 돌려준다.

```json
{"results":[{"session_id":50,"status":"killed","message":null},
            {"session_id":999999,"status":"error","message":"이미 종료된 세션입니다."}]}
```

요청 바디 클래스 `RdbKillSessionRequest` 는 record 가 아니라 `@JsonCreator` 를 단 평범한 final
클래스다 — 그래야 원본(Java 17)과 AIX(Java 8)에 **같은 파일을 그대로** 둘 수 있다. 세션 id 를 Long
이 아니라 String 으로 받는 이유는, 숫자가 아닌 값이 오면 Jackson 이 400 을 먼저 던져서 우리
메시지("세션 ID가 올바르지 않습니다.")가 나갈 기회가 없기 때문이다.

### 12-2. Lock 화면 = Holder/Waiter 트리

탭 이름도 오라클의 `Lock Holder/Waiter Tree` 에 맞춰 **`Lock Holder/Waiter`** 로 바꿨다.

**백엔드는 그대로 "대기 1 : 유발 1" 쌍의 평평한 목록을 주고, 트리는 화면에서 만든다.** 서버 쿼리를
늘리지 않고도 임의 깊이의 체인을 그릴 수 있어서다.

- **루트 = 아무도 기다리지 않는 세션 = 진짜 Holder.** 그 아래에 그 세션을 기다리는 Waiter 를 붙인다
- A→B→C 다단계 체인이 그대로 중첩된다. 중간 노드는 `BOTH` 배지(막고 있으면서 자신도 기다리는 중)
- PostgreSQL 은 `pg_blocking_pids()` 가 배열이라 한 대기 세션에 유발 세션이 여럿일 수 있고, 그 경우
  같은 노드가 여러 부모 아래에 나타난다
- **순환(교착 직전)이면 루트가 하나도 없다.** 그대로 두면 화면이 비어 "락이 없다" 로 읽히므로,
  가장 오래 기다린 세션을 임시 루트로 세우고 `⚠ 순환 구조` 경고를 띄운다. 재귀는 방문 경로로 끊는다
- 건수는 쌍이 아니라 **관련 세션 수**로 센다(A→B→C 는 쌍 2개지만 얽힌 세션은 3개다)

트리에 유발 세션의 호스트도 보여주려고 백엔드 Lock 쿼리 3종에 `blocker_host` 를 추가했다.

실측(PostgreSQL 3단 체인):

```
HOLDER 175   active                       BEGIN; UPDATE ... id=1; SELECT pg_sleep(400); COMMIT;
└─ BOTH 182  Lock / transactionid  58.64  BEGIN; UPDATE ... id=2; ... UPDATE ... id=1; COMMIT;
   └─ WAITER 189  Lock / transactionid  57.40  BEGIN; ... UPDATE ... id=2; COMMIT;
```

189 를 Kill 하니 182 가 **`BOTH` → `WAITER` 로 자동 변경**됐다(더 이상 아무도 막지 않으므로).

### 12-3. 화면에서 신경 쓴 것

- **자동 새로고침이 체크를 풀지 않는다.** 선택 집합을 DOM 밖(`Set`)에 들고 있다가 다시 그린 뒤
  복원한다. 오라클 화면의 TM Lock 탭이 이 처리가 빠져 5초마다 체크가 풀리던 버그가 있었다
  (`dbagent-java-overview` 메모리 기록). 화면에서 사라진 세션 id 는 선택 집합에서도 지운다
- **확인창·결과창을 `window.confirm`/`alert` 대신 페이지 모달로** 만들었다. 브라우저 기본
  대화상자는 페이지의 모든 이벤트를 막아 자동화 검증이 그 지점에서 멈추고, 무엇보다 **"무엇을
  죽이는지"를 목록으로 보여줄 수가 없다.** 확인창은 대상 세션 ID 를 전부 나열한다
- **모달이 떠 있는 동안 자동 새로고침을 멈춘다.** 안 그러면 사용자가 방금 고른 대상이 눈앞에서 바뀐다
- 결과는 **건별 status 를 세어** "성공 n건 / 실패 m건" + 실패 사유까지 보여준다. 건별 확인 없이
  "전송했습니다" 만 띄우면 실패를 성공으로 오해한다(2026-08-30에 오라클 화면에서 실제로 겪은 버그)
- Kill 후에는 성공/실패와 무관하게 선택을 비운다 — 남겨두면 이미 사라진 세션 id 가 계속 선택된
  것처럼 보인다

### 12-4. 검증

관리자/일반 계정을 각각 만들어 실제로 확인했다(검증 후 두 저장소 모두에서 계정 삭제).

| 항목 | 결과 |
|---|---|
| 관리자 Kill (4엔진) | 전부 `killed` + 대상 세션이 실제로 사라짐 |
| **일반 계정 Kill** | **403** `세션 Kill 권한이 없습니다. 관리자만 사용할 수 있습니다.` — 세션 생존 확인 |
| 토큰 없이 Kill | 403 (같은 메시지) |
| 일반 계정 조회 | 정상 — 조회는 되고 Kill 만 막힌다 |
| 일반 계정 화면 | Kill 버튼·체크박스·전체선택이 전부 없음(DOM 에서 제거), 안내 문구도 Kill 문장이 빠짐 |
| 없는 세션 Kill | MySQL/PostgreSQL 모두 `이미 종료된 세션입니다.` |
| `sessions:["1 OR 1=1"]` | `세션 ID가 올바르지 않습니다.` — DB 까지 가지 않음 |
| 빈 목록 / 없는 db_id | 400 / 404 |
| 자동 새로고침 중 체크 유지 | 유지됨 |
| 모달 중 자동 새로고침 | 멈춤 |
| UI Kill 전체 흐름 | 체크 → 확인창(대상 ID 표시) → 실행 → "성공 1건 / 실패 0건" → 트리 재계산 |
| 브라우저 JS 에러 | 0건 |

AIX 이관본(Java 8 / Spring Boot 2.7)도 8007 로 임시 기동해 4엔진 Kill + 권한 403 을 동일 확인.

### 12-5. 변경 파일

두 저장소 동일. 신규 1개 + 수정 8개.

| 파일 | 내용 |
|---|---|
| `rdb/RdbKillSessionRequest.java` | **신규** — Kill 요청 바디(양쪽 프로젝트 동일 파일) |
| `rdb/EngineMonitorService.java` | `killSession()` 추가, Lock 공통 키에 `blocker_host` |
| `rdb/MySqlMonitorService.java` | `KILL <id>` + 1094 안내, Lock 쿼리 2종에 `blocker_host` |
| `rdb/PostgresMonitorService.java` | `pg_terminate_backend` (false 구분), `blocker_host` |
| `rdb/MsSqlMonitorService.java` | `KILL <spid>`, `blocker_host` |
| `rdb/RdbMonitorController.java` | `POST /kill_session` — **isAdmin 게이트** + 건별 id 검증 |
| `static/rdb-session-views.js` | 체크박스·Kill 버튼·확인/결과 모달, Lock 표 → Holder/Waiter 트리 |
| `static/dbagent-common.js` | rdb_locks 라벨을 `Lock Holder/Waiter` 로 |
| `static/mysql`/`postgres`/`mssql-overview-dashboard.html` | 탭 라벨 변경, 캐시버스터 |

**캐시버스터** — `dbagent-common.js?v=5`, `rdb-session-views.js?v=2`. 두 번호 모두 두 저장소
히스토리에서 미사용 확인.

**빌드/배포** — 두 저장소 `mvn clean package`(stale 0건) → dist 3곳 jar 교체 → 원본 8006 재기동 →
서빙 콘텐츠 == 소스 확인.

### 12-6. 남은 사항

- Kill 은 **세션 단위**다. MySQL 의 `KILL QUERY`(커넥션은 두고 쿼리만 취소)는 넣지 않았다 —
  필요하면 같은 엔드포인트에 모드 파라미터를 더하면 된다.
- 오라클 화면에 있는 "장애조치 버튼"(블로킹 holder 일괄 kill) 같은 자동 조치는 만들지 않았다.
  RDB 쪽은 사용자가 트리에서 직접 고르는 방식만 제공한다.

---

## 13. RDB 대시보드 "용량 조회" 탭 (Oracle 테이블 스페이스 조회 대응)

> 사용자 요청: **"테이블 스페이스 용량 조회 메뉴 만들어줘"**
> 두 저장소 동일 적용 · 빌드 → dist 3곳 배포 → 실화면 검증 완료

### 13-1. 무엇을 "테이블스페이스" 로 볼 것인가

오라클은 **테이블스페이스 → 데이터파일** 2단이다. 그런데 RDB 쪽에는 그 단위가 없거나 무의미하다.

| 엔진 | 테이블스페이스가 있나 | 이번에 1단으로 잡은 것 |
|---|---|---|
| MySQL / MariaDB | **없다.** InnoDB file-per-table 은 테이블 하나가 파일 하나일 뿐 묶음이 아니다 | 스키마(=데이터베이스) |
| PostgreSQL | **있다**(`pg_tablespace`). 하지만 기본 구성은 `pg_default` 하나뿐이라 1단으로 잡으면 행이 한 줄이다 | 접속한 DB의 스키마 |
| MS SQL Server | **사실상 같다.** DB 마다 데이터 파일을 미리 할당해 두고 그 안을 채워 쓴다 | 데이터베이스 |

그래서 같은 2단 구조를 **"저장 단위 → 테이블"** 로 대응시켰다. DBA 가 실제로 알고 싶은 것도
"어느 스키마/DB 가, 그 안의 어느 테이블이 용량을 먹고 있나" 이지 파일 경로가 아니다.

탭 이름은 **"용량 조회"** 로 했다. MySQL/PostgreSQL/MS SQL 은 이걸 테이블스페이스라고 부르지
않으므로 그 이름을 그대로 쓰면 오해를 부른다. 대신 화면 상단 안내 문구에 엔진별로 무엇을 센
숫자인지 서버가 내려준 설명(`note`)을 그대로 띄운다.

### 13-2. 엔드포인트

| API | 내용 |
|---|---|
| `GET /api/rdb/capacity` | 1단 — 저장 단위별 용량 (`{unit, note, rows}`) |
| `GET /api/rdb/capacity_detail?scope=` | 2단 — 그 안의 테이블별 용량 (`{scope, note, rows}`) |

`unit`("스키마"/"데이터베이스")을 응답에 담아 화면이 **첫 컬럼 머리글을 바꿔 단다.** 이렇게 하면
이 파일에도 `db_type` 분기가 생기지 않는다(11절과 같은 원칙).

### 13-3. 없는 값은 억지로 채우지 않는다

**할당·여유·사용률은 MS SQL 에서만 나온다.** 네 엔진 중 MS SQL 만 오라클처럼 파일을 미리 할당해
두고 그 안을 채워 쓰기 때문이다. MySQL/PostgreSQL 은 "할당" 개념이 없으므로 `total_mb` 와
`used_pct` 를 **null 로 두고 화면에 `-`** 로 그린다.

> 기존 DASHBOARD 탭의 Storage 패널(`/api/rdb/storage`)은 `total==used, free=0` 으로 채워서
> **사용률이 항상 100%** 로 나온다. 그건 정보가 아니라 잡음이다. 새 API 는 그 실수를 반복하지
> 않으려고 null 을 쓴다. (기존 패널은 건드리지 않았다 - DASHBOARD 탭 레이아웃에 묶여 있다)

컬럼 의미는 이렇게 고정했다.

| 컬럼 | MySQL/MariaDB | PostgreSQL | MS SQL |
|---|---|---|---|
| 테이블 수 | ✓ | ✓ | ✓ |
| 데이터 / 인덱스(MB) | data_length / index_length | pg_table_size / pg_indexes_size | 사용 페이지의 데이터/인덱스 분리 |
| 사용(MB) | data+index | pg_total_relation_size | reserved 페이지 |
| **할당(MB)** | — | — | 데이터 파일(ROWS) 할당량 |
| 여유(MB) | InnoDB data_free(추정) | — | 할당 − 사용 |
| **사용률** | — | — | 사용/할당 |

MS SQL 은 트랜잭션 로그 파일을 이 표에서 뺐다 - 성격이 달라 같이 더하면 사용률이 왜곡된다.
tempdb 를 포함한 시스템 DB 는 **일부러 보여준다**(용량 장애는 tempdb 에서 시작되는 경우가 많다).

### 13-4. MS SQL 만 특별했던 점

할당량은 `sys.master_files` 로 한 번에 읽히지만 **실제 사용량은 DB 안에 들어가야** 볼 수 있다
(`dm_db_partition_stats` 는 DB 단위 DMV). DB 마다 따로 질의하면 왕복이 DB 수만큼 늘어나므로,
3-part naming 으로 각 DB 를 가리키는 SELECT 를 `UNION ALL` 로 이어 붙인 문장 하나를 만들어
한 번에 실행한다.

- DB 이름은 `QUOTENAME()` 으로 감싼다(대괄호 이스케이프). 이 이름들은 사용자 입력이 아니라
  `sys.databases` 에서 방금 읽은 값이다
- **`HAS_DBACCESS` 필터가 없으면 권한 없는 DB 하나 때문에 문장 전체가 실패한다**

### 13-5. 주입 방어

`scope` 는 사용자가 고른 값이 SQL 로 가는 자리다.

- MySQL / PostgreSQL: `PreparedStatement` 바인딩
- MS SQL: `sp_executesql` 파라미터로 넘겨 `QUOTENAME()` 으로 감쌈 + `sys.databases` 존재 확인

실측으로 `scope=testdb]; DROP TABLE dbo.cap_test; --` 를 넣어 봤다 → **0건 반환, 테이블
20,000행 그대로 생존.**

### 13-6. 자동 새로고침

처음엔 60초 제한(`throttleMs`)으로 넣었다가, **자동 갱신 대상에서 아예 빼는 것으로 바꿨다**
(사용자 지시 - 14-3절). 용량은 초 단위로 변하지 않는데 쿼리는 전 테이블의 크기를 훑어 무거워서
주기적으로 돌려도 얻는 것이 없다.

### 13-7. 화면

- 합계 행(`tfoot`) — 오라클 테이블스페이스 화면의 "전체 할당량/사용량" 요약에 대응. **사용률은
  더할 수 있는 값이 아니라** 전체 사용/전체 할당으로 다시 계산한다
- 사용률 칸에 막대를 깔았다(75% 이상 주황, 90% 이상 빨강) - 숫자만으로는 "찼는지" 가 눈에 안 들어온다
- 행을 클릭하면 그 단위 안의 테이블별 용량을 모달로 연다(1단 → 2단 드릴다운)
- 이 탭은 Kill 대상이 아니라 체크박스·Kill 버튼을 붙이지 않는다

### 13-8. 검증

로컬 4엔진에 각각 2만 행짜리 `cap_test` 테이블을 만들어 확인했다.

| 항목 | 결과 |
|---|---|
| MySQL 8.4.11 | 스키마 1건, 드릴다운 2건 |
| MariaDB 11.8.9 | 스키마 1건, 드릴다운 2건 |
| PostgreSQL 16.15 | 스키마 2건(public/app2), 드릴다운 2건 |
| MS SQL 2022 | 데이터베이스 5건(시스템 DB 포함), 드릴다운 2건 |
| MS SQL 할당/사용률 | `msdb 91.10%`(빨강 막대), `master 80.00%`(주황), 나머지 초록 |
| MySQL/PG 할당·사용률 | `-` 로 표시 (억지 100% 아님) |
| 합계 행 | 5개 DB 합계 사용률 19.35% - 재계산 확인 |
| 주입 시도 | 0건 + 테이블 생존 |
| scope 누락 / 토큰 없이 | 400 / 403 |
| 없는 스코프 | 0건 + 안내 문구 |
| 자동 새로고침 제한 | 연속 호출해도 갱신 시각 그대로, 탭 클릭 시 즉시 갱신 |
| 메뉴 표시 설정 | `rdb_capacity` 숨김 시 탭 사라지고 DASHBOARD 로 자동 이동 |
| 브라우저 JS 에러 | 0건 |

AIX 이관본(Java 8 / Spring Boot 2.7)도 8007 로 임시 기동해 4엔진 × 2 API 동일 확인.

### 13-9. 변경 파일

두 저장소 동일. 수정 8개(신규 없음).

| 파일 | 내용 |
|---|---|
| `rdb/EngineMonitorService.java` | `getCapacity()` / `getCapacityDetail()` + 컬럼 의미 규약 |
| `rdb/MySqlMonitorService.java` | 스키마별/테이블별 용량 |
| `rdb/PostgresMonitorService.java` | 스키마별/테이블별 용량 |
| `rdb/MsSqlMonitorService.java` | DB별 할당·사용(UNION ALL 3-part naming) / 테이블별 |
| `rdb/RdbMonitorController.java` | `GET /capacity`, `GET /capacity_detail` |
| `static/rdb-session-views.js` | 용량 탭 렌더링, 합계 행, 사용률 막대, 드릴다운 모달, 갱신 제한 |
| `static/dbagent-common.js` | `rdb_capacity` 탭/메뉴 등록 |
| `static/mysql`/`postgres`/`mssql-overview-dashboard.html` | 탭 버튼 + 뷰 컨테이너 |

**캐시버스터** — `dbagent-common.js?v=6`, `rdb-session-views.js?v=3`(두 저장소 히스토리 미사용 확인).

> **주의 — `sed -i ... *.html` 을 쓰지 말 것.** 12절 작업 때 이 명령이 손대지도 않은 HTML 5개의
> 줄바꿈(CRLF→LF)까지 바꿔 git 에 잡혔다. 내용은 같은데 diff 만 부풀어 되돌리고 재빌드해야 했다.
> 이번엔 파일을 개별로 지정해 `perl -0pi -e` 로 바꿨고, 변경 파일이 의도한 것만 남는지
> `git status` 로 확인했다.

### 13-10. 남은 사항

- DASHBOARD 탭의 기존 Storage 패널(`/api/rdb/storage`)은 그대로 뒀다. 사용률이 항상 100% 로
  나오는 문제가 있지만 그 탭 레이아웃에 묶여 있어 이번 범위에서 제외했다 - 정확한 값은 용량 조회
  탭에서 본다.
- PostgreSQL 의 진짜 테이블스페이스(`pg_tablespace`)와 MS SQL 의 파일/파일그룹 목록은 넣지 않았다.
  필요하면 2단 옆에 탭을 하나 더 두는 식으로 붙일 수 있다.
- MySQL 의 `data_free` 는 통계 기반이라 정확한 값이 아니다(화면 안내에 명시).

---

## 14. Interval 연동 확인 + 강제 새로고침 버튼

> 사용자 질문: "세션리스트, Lock Holder/Waiter 메뉴도 interval 항목 연동 되었어?"
> 이어진 요청: "강제 새로고침 하나 만들어줘"

### 14-1. Interval 연동 — 되어 있다 (실측 확인)

세 탭 모두 필터바의 Interval 셀렉트를 그대로 탄다. 경로는 이렇다.

```
Interval 셀렉트 → setInterval → refreshAllPanels()
                              → window.dbagentRefreshRdbSessionViews()
                              → 지금 보고 있는 탭만 다시 읽음
```

**보이는 탭만 갱신한다** - 세 탭을 항상 다 돌리면 안 보는 화면 때문에 DB 에 매 주기 쿼리가 배로
나간다(세션/락 쿼리는 둘 다 시스템 뷰 전체를 훑는다). 백그라운드 탭이면 `document.hidden` 으로
건너뛰는 것도 기존 동작 그대로다.

브라우저 실측(MySQL 대시보드):

| 항목 | 결과 |
|---|---|
| 세션 리스트, Interval=5s | `11:26:45 → 11:26:50 → 11:26:55` 5초마다 갱신 |
| Lock Holder/Waiter, 5s | `11:27:18 → 11:27:23` 갱신 |
| Interval=off | 8초 기다려도 시각 그대로 (멈춤) |
| 용량 조회, 5s | **일부러 안 돈다** - 60초 제한(13-6절) |

### 14-2. 강제 새로고침 버튼 (신규)

세 탭 툴바 오른쪽에 `⟳ 새로고침` 을 넣었다. **Interval 설정(off 포함)과 용량 탭의 60초 제한을
무시하고 즉시 다시 읽는다.** 조회 동작이라 관리자 전용이 아니다.

- 누르는 동안 버튼을 비활성화하고 아이콘을 회전시킨다 → 그러려고 `loadSessions`/`loadLocks`/
  `loadCapacity` 가 fetch 프로미스를 **반환하도록** 바꿨다(전엔 fire-and-forget 이었다)
- 누를 때 그 탭의 `lastLoadAt` 도 같이 민다 - 안 그러면 방금 눌렀는데 자동 갱신이 바로 뒤따라 돈다
- `guardNoDbId` 로 일찍 빠져나가면 프로미스가 아니므로 `typeof p.then` 을 확인한다

실측:

| 항목 | 결과 |
|---|---|
| Interval=off 상태에서 Lock 탭 새로고침 | `11:27:23 → 11:27:35` 갱신됨 |
| 용량 탭 60초 제한 중 새로고침 | `11:27:55 → 11:28:10` 제한 무시하고 갱신됨 |
| 버튼 존재 | 세 탭 모두 `⟳ 새로고침` |
| 브라우저 JS 에러 | 0건 |

**캐시버스터** — `rdb-session-views.js?v=4`(두 저장소 히스토리 미사용 확인). 변경은 이 파일과
세 대시보드 HTML 의 버전 숫자뿐이고, 백엔드는 손대지 않았다.

### 14-3. 용량 조회는 Interval 연동에서 제외 (사용자 지시)

> "용량 조회는 interval 연동 필요 없어, 새로 고침만 있으면 됨"

13-6절에서 60초 제한으로 넣었던 것을 **자동 갱신 대상에서 아예 제외**하는 것으로 바꿨다.

- `TABS` 의 `throttleMs: 60000` → **`autoRefresh: false`**
- `dbagentRefreshRdbSessionViews()` 는 이 표시가 있는 탭을 그냥 건너뛴다
- 제한이 없어졌으므로 `lastLoadAt` 을 밀던 코드(`showView`, `wireReloadButton`)도 걷어냈다
- **화면에 이유를 밝힌다** - 안 밝히면 시각이 안 바뀌는 것을 고장으로 오해한다. 서버가 준 엔진별
  안내 문구 뒤에 "이 탭은 Interval 자동 새로고침 대상이 아닙니다(용량은 초 단위로 변하지 않고
  조회가 무겁습니다). 탭을 열 때와 새로고침 버튼을 누를 때만 다시 읽습니다." 를 덧붙인다

실측(MS SQL 대시보드, Interval=5s):

| 항목 | 결과 |
|---|---|
| 용량 조회 20초 관찰 | `11:33:27` 그대로 - 자동 갱신 **전혀 없음** |
| 용량 조회 새로고침 버튼 | `11:33:27 → 11:33:49` 갱신됨 |
| 대조군: 세션 리스트 | `11:33:52 → 11:33:57` 5초마다 갱신(종전대로) |
| 브라우저 JS 에러 | 0건 |

**캐시버스터** — `rdb-session-views.js?v=5`. 백엔드는 손대지 않았다.

---

## 15. 다음 작업 예정 (2026-09-06) — 대시보드 UI 개편 + 문서 6절 지표 적용

> 사용자 예고: "내일은 대시보드 UI 변경하면서 6번부터 적용하지 않은 부분 적용할거야"
> 대상 문서: `세션리스트 및 세션 정보 조회 쿼리.md` **6절 — 대시보드에 추가해야 할 핵심 지표**

09-05 까지 붙인 것은 세션 리스트 / Lock Holder-Waiter / 용량 조회 세 화면이다. 6절의 지표 5종은
6.4(버퍼 캐시 히트율)만 KPI 카드에 들어가 있고 나머지는 미구현이거나 **값은 있는데 화면이 없다.**

### 15-1. 착수 전 결론: 대부분 새 쿼리가 아니라 "표시"가 없는 것이다

기존 `/api/rdb/{mysql,postgres,mssql}_status` 응답에 원자재가 상당수 이미 들어 있다.
**새 쿼리를 짜기 전에 이 키들부터 확인할 것.** 아래는 09-05에 응답 키를 직접 훑어 정리한 결과다.

| 문서 절 | 상태 | 무엇이 있고 무엇이 없나 |
|---|---|---|
| **6.1** 커넥션 풀 고갈율 | 값 거의 있음 → **게이지만** | MySQL `threadsConnected`/`maxConnections`, PG `currentConnections`/`maxConnections` 모두 있음. **MS SQL 만 분모가 없다** → `@@MAX_CONNECTIONS` 또는 `sys.configurations`('user connections', 0=무제한) 추가 |
| **6.2** 장기 트랜잭션 | **백엔드 신규 필요** | PG 는 `idleInTxCount`(건수)만 있고 **경과 시간 임계치가 없다**. MySQL `information_schema.innodb_trx`, MS SQL `sys.dm_tran_active_transactions` 는 아예 미조회 |
| **6.3** 리플리케이션 지연 | **전부 없음 + 로컬 검증 불가(확정)** | **로컬에 복제 구성은 만들지 않기로 확정**(2026-09-05 사용자 지시). 실제 지연 값은 회사 환경에서만 볼 수 있다 |
| **6.4** 버퍼 캐시 히트율 | 값 있음, **트렌드만 없음** | 세 엔진 KPI 카드에 현재값이 있다. 문서가 요구하는 "실시간 트렌드 선 그래프"는 이 앱에 시계열 저장소가 없어, **프론트에서 폴링 값을 누적**하는 방식(새로고침하면 초기화)이 현실적 |
| **6.5** 임시 공간/로그 용량 | **부분 있음** | PG `tempFiles`/`tempBytes`, MySQL `createdTmpDiskTables`/`tmpDiskTablePct` 있음. MS SQL 은 `tempdbDataMb`/`tempdbLogMb` 만 있고 **여유 공간(`sys.dm_db_file_space_usage`)과 Percent Log Used 가 없다** |

**6.3 은 로컬에서 재현하지 않는다.** 복제 구성을 만들지 않기로 했으므로, 이 지표를 넣더라도 이 PC
에서 확인할 수 있는 것은 "복제 구성 없음" 응답까지다. 넣기로 한다면 **복제가 없을 때 화면이 오류가
아니라 "복제 구성 없음" 으로 조용히 표시되는지**가 로컬 검증 범위이고, 실제 지연 값은 회사 환경에서
확인한다. 착수 시 "이번 범위에 넣을지" 를 먼저 정할 것.

**6.2 의 함정 — 세션 리스트로 대체할 수 없다.** 세션 리스트는 유휴 세션을 제외하므로
"트랜잭션만 열어둔 채 방치된 세션"이 **목록에서 빠진다.** 6.2 는 바로 그걸 잡는 지표다.

### 15-2. UI 를 손대는 김에 정리할 부채 (09-05에 발견)

- **DASHBOARD 탭 Storage 패널의 사용률이 항상 100%다.** `/api/rdb/storage` 가 `total==used, free=0`
  으로 채우기 때문이다(13-3절). 용량 조회 탭은 이 실수를 피하려고 null 을 쓴다. UI 개편 때 이
  패널을 용량 조회 쪽 값으로 바꾸거나 걷어낼 것
- **`overviewView` 마크업이 죽은 채 남아 있다.** 2026-09-04 에 Detail 탭을 지우면서
  `display:none` 으로 고정만 해 뒀는데, 그 안의 KPI 카드가 `detailView` 쪽과 **id 만 다른 중복**이다
  (`kpiUptime`/`kpiUptime2` 등 4쌍, 세 대시보드 모두). UI 개편이면 정리할 적기다
- **MS SQL 용량 조회 쿼리가 `log_mb` 를 SELECT 하고도 응답에 담지 않는다**(죽은 컬럼).
  6.5 의 로그 용량에 그대로 쓸 수 있다

### 15-3. 지킬 원칙 (이미 잡혀 있는 것)

- **엔진 차이는 백엔드에서 정규화한다.** 프론트(`rdb-session-views.js`)에 `db_type` 분기를 만들지 말 것
- **값이 없는 엔진은 null → 화면 '-'.** 억지로 채우면 6.1/6.5 에서도 "항상 100%" 류 잡음이 생긴다
- 탭을 추가하면 고칠 곳 4군데: `dbagent-common.js` 의 `RDB_TAB_BUTTONS` + `DBAGENT_MENU_GROUPS`,
  세 대시보드 HTML 의 `.view-tabs`, `rdb-session-views.js` 의 `TABS`
- 무거운 조회는 Interval 자동 갱신에서 빼고 새로고침 버튼만 둔다(`autoRefresh:false`) —
  용량 조회와 같은 판단(14-3절)
- 두 저장소 동시 적용, 캐시버스터는 히스토리에서 쓴 적 없는 번호로(`git log -S` 확인)
